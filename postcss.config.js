module.exports = {
  plugins: {
    // Tailwind CSS v4 — the PostCSS plugin now handles imports and
    // vendor-prefixing internally, so `autoprefixer` is no longer needed.
    '@tailwindcss/postcss': {},
  },
}
