/**
 * Foundation Tabs — re-exports the shadcn/ui Tabs components so all existing
 * imports from `@/components/foundation` get the real shadcn system.
 * Custom styling applied via className to match the trading terminal register.
 */
export { Tabs, TabsList, TabsTrigger, TabsContent } from '@/components/ui/tabs'
