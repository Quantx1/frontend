# F&O Lab — strategy workspace

> Part of the Quant X as-built screen inventory — see [`../03-SCREEN-INVENTORY.md`](../03-SCREEN-INVENTORY.md)
> for the full index and [`../01-DESIGN-SYSTEM.md`](../01-DESIGN-SYSTEM.md) for every token used below.

**11 surfaces.**

## Family notes

SOURCE OF TRUTH: /Users/rishi/QuantX/frontend/components/fno/FoStrategiesWorkspace.tsx (2558 LOC) mounted from /Users/rishi/QuantX/frontend/app/(platform)/fno/page.tsx at TabsContent value="lab". The former /fo-strategies route 301s to /fno?tab=lab.

HOST CHROME (identical on every entry below): AppShell = fixed left sidebar 240px expanded / 68px collapsed (bg #151517), fixed right rail 72px, main pane scrolls with inner cap max-w-[1440px] and px-16 / md:px-24 gutters. Page body is `space-y-6 px-4 py-5 md:px-6 xl:px-8`. Outer FnoHubPage PageHeader: eyebrow "AI Derivatives Desk · F&O" (Geist Mono 11px, uppercase, letter-spacing 0.1em, #96969E), h1 "F&O Desk" at text-display-sm (40px/1.1, -0.02em, weight 400) with an inline warning Badge reading "Elite" (bg #F0A94F/10, ink #F0A94F, radius 9999), description "Agents read the derivatives tape live: index snapshots · per-stock OI scanners · option-chain heatmap · payoff explorer.", right action Button variant="ai" (glass-control, ink #8FB0FF) "Ask AI" with Sparkles 16px. Then Reveal(delay 0.08) wrapping a Card with p-4 (16px) that contains the OUTER 6-tab rail: Overview · Analysis · Stock Scanners · OI Tracker · Payoff Calc · Strategy Lab, each trigger carrying a 16px lucide icon at mr-4px. Tab rail = inline-flex gap-4px, radius 9999, 1px #29292D border, bg #0D0D0E, p-4px; triggers px-14 py-6, 14px/500, muted #96969E; active trigger bg #151517, ink #F7F7F8, shadow-sm. Page footer p (border-t #29292D, pt-16, 10px, #96969E): "F&O data via admin Kite (option chain, indices) + NSE live FII/DII API. When provider feeds are down, panels show the source/error tag rather than synthetic numbers. Lot sizes: NSE Jan-2026 revision." followed by DisclaimerFooter standard: "For educational purposes. Not investment advice. Markets carry risk."

NESTED TABS: the Strategy Lab body renders its OWN 4-tab rail (Recommendations · Open · Closed · Chain) inside the outer 6-tab rail — two concentric pill rails, both radius 9999. Recommendations/Open carry a primary Badge count; Closed carries a muted Badge count; Chain carries none.

TOKENS AS BUILT (verified against app/globals.css + lib/tokens.ts, exactly matching the supplied system): dark page #0D0D0E (--color-main), card #151517 (--color-wrap), elevated/hover #1E1E21 (--color-wrap-hover == --color-surface-2), hairline #29292D (--color-line), accent border #3B3B40 (--color-wrap-line). Light: #EDF1F4 / #FFFFFF / #F4F7F9 / #DDE5ED / #C8D4DE. Ink #F7F7F8 / #D3D3D7 (--color-desc) / #96969E (--color-muted); light #1D1D1D / #4D585F / #5F6B75. Accent FILL #406AE4 (hover #3055C2, white ink) via `.glass-control-accent` which also carries elev-1; accent INK #8FB0FF dark / #3459C9 light (Tailwind `text-primary` resolves to the INK). P&L only: up #10B981 dark / #0A6B50 light, down #F5808C / #B81C22, warning #F0A94F / #9A4D00. --chart-primary = #8FB0FF dark / #406AE4 light. --color-ai == the accent ink. Radii: xs 6 · sm 8 · md 12 · lg 16 · full 9999 · mark 2. Elev-1 = 0 1px 2px rgba(0,0,0,.30), 0 4px 12px -6px rgba(0,0,0,.40); elev-2 and elev-3 heavier. Durations 90/160/240/380ms.

FOUNDATION PRIMITIVE ANATOMY (referenced repeatedly below):
· Card — radius 12, 1px #29292D, bg #151517, shadow-none by default (elevation="flush"); density default sets --card-px 20px / --card-py 16px. CardHeader = min-h 44px, flex, border-b #29292D, title slot 16px/22px weight 600, tracking -0.01em. Card MAY NOT nest a Card (rule in Card.tsx) — nested content uses bordered wells instead.
· Button — radius 8 (rounded-sm), weight 600; sm = h-28 gap-6 px-12 text-12; md = h-32 gap-8 px-14 text-13; lg = h-40 px-20 text-14. primary = .glass-control-accent (#406AE4 fill, white ink, elev-1); secondary = .glass-control (#1E1E21 fill, 1px #29292D); ghost = transparent + #D3D3D7 ink, hover bg #1E1E21; ai = .glass-control with #8FB0FF ink. active:scale(.98), 90ms.
· Badge — radius 9999, px-10 py-2, 12px/500, tinted: primary bg accent/10 ink accent-INK border accent/20; up/down/warning likewise; muted bg #1E1E21 ink #96969E border #29292D.
· StatCard — radius 8, 1px #29292D, bg #151517, p-16, gap-8; label Geist Mono 11px uppercase 0.1em #96969E; value Geist Mono 24px weight 400 tabular-nums; loading = 60%×28px pulse bar at #29292D/80.
· EmptyState (size md) — recessed well, NO border, bg #1E1E21, radius 12, p-40, gap-12, centered; icon in a 44×44 circle (1px #29292D, bg #151517) tinted #96969E; title 16px/22px weight 600; description 14px/21px #D3D3D7 capped at 42ch.
· Dialog (Radix) — overlay fixed inset-0 z-40 rgba(0,0,0,.60); content fixed centered z-50, w-full, radius 8, 1px #29292D, bg #151517, p-20, shadow elev-1; title mb-8, 16px weight 400; sr-only description. Width is overridden per call site with !max-w-*.
· NumericInput — wrapper flex, radius 8, 1px #29292D, bg #1E1E21; input h-40 (md) 14px, px-12; right stepper column 28px wide split into +/− halves divided by a 1px #29292D rule, glyphs 12px #96969E. Wheel-scroll is blocked (blurs on wheel).
· Skeleton — animate-pulse, bg #29292D at 80%, radius 6 by default.
· Toast — Sonner, mounted globally in app/providers.tsx; every mutation in this workspace reports through it.

TWO NOTABLE AS-BUILT FACTS THE BRIEF ANTICIPATED DIFFERENTLY:
1. There is NO payoff diagram anywhere inside FoStrategiesWorkspace. The strategy builder's only economics preview is a single "Net per share" Debit/Credit line computed client-side from selected LTPs. The payoff explorer is a SIBLING outer tab (/fno?tab=payoff → components/fno/PayoffCalculator.tsx) and is never rendered inside the lab. The chart surfaces that DO exist in this file are Volatility Smile (h=360), Term Structure (h=340) and Vol Cone (h=360), all Recharts ComposedChart.
2. There is NO confirmation dialog on Close. The Close button fires POST /api/fo-strategies/paper/{id}/close immediately on click; the only "confirm" affordance is the in-button spinner. The three real dialogs are Deploy-confirm (Radix, !max-w-md 448px), Backtest (Radix, !max-w-2xl 672px), AI-suggest (Radix, !max-w-xl 576px), plus one hand-rolled non-Radix overlay for Adjust (max-w-2xl 672px, z-50, bg-black/70, radius 16, bg #0D0D0E).

DATA / REFRESH CADENCE: recommendations SWR key "fo:overview" refreshInterval 300000ms, deduping 60000ms; positions key "fo:paper:positions" refreshInterval 30000ms, deduping 10000ms (this is the live MTM clock); chain key ["fo:chain",symbol] 30000ms/15000ms; term ["fo:term",symbol] 60000ms/30000ms; cone ["fo:cone",symbol] 300000ms/60000ms. All have revalidateOnFocus:false and keepPreviousData:true, so a tab switch never blanks a populated surface.

MONEY FORMATTING (lib/format.ts) — reproduce exactly: Indian 2-2-3 grouping via Intl 'en-IN'. Nulls render as EM DASH "—", never "-" or "--". Negative money uses U+2212 MINUS "−", never a hyphen, so columns align under tabular-nums. P&L in this file uses inrSigned → always an explicit sign: "+₹12,340" / "−₹8,150". Absolute risk uses a bare "₹" with no sign. Percentages here are hand-formatted with a leading "+" and toFixed(1).

STRATEGY_LABEL map (drives every heading in the file): iron_condor→"Iron Condor", bull_call_spread→"Bull Call Spread", bear_put_spread→"Bear Put Spread", long_straddle→"Long Straddle", short_strangle→"Short Strangle", iron_butterfly→"Iron Butterfly". Unknown slugs fall through to the raw slug.

AUTH: every entry sits in the (platform) route group behind Supabase auth; the layout redirects to /onboarding/broker-connect when onboarding is incomplete. The Chain sub-surface additionally requires a linked broker (Zerodha / Upstox / Angel) and swaps its entire body for a broker-connect EmptyState when source === 'unavailable'.

---

## `/fno?tab=lab` — Strategy Lab — workspace shell (header, KPI strip, regime strip, copilot, tab rail)

**File** `/Users/rishi/QuantX/frontend/components/fno/FoStrategiesWorkspace.tsx (L261-372, L934) + /Users/rishi/QuantX/frontend/app/(platform)/fno/page.tsx + /Users/rishi/QuantX/frontend/components/fno/OptionsCopilotCard.tsx` · **118 LOC** · **Access** Authenticated. Redirects to /onboarding/broker-connect when onboarding incomplete. KPI tiles render skeletons until the first positions payload lands.

**Shell** — AppShell (240px left sidebar, 72px right rail, 1440px content cap) → FnoHubPage PageHeader → Card p-16 → outer 6-tab rail → TabsContent 'lab' (mt-16) → FoStrategiesWorkspace root div `w-full space-y-5` (20px vertical rhythm between all five shell blocks).

**Purpose.** The persistent frame every other lab sub-surface renders inside: a second-level PageHeader with the two global actions, a four-tile aggregate KPI strip over the paper book, a one-line live regime/VIX context strip, the user-triggered AI Options Copilot card, and the inner four-tab rail. Nothing here is a route of its own — it is always on screen behind the four inner tabs.

### Anatomy (render order)

| # | Region | What renders |
|---|---|---|
| 1 | **PageHeader (nested, second on the page)** | flex-col gap-16, border-b 1px #29292D, px-16 py-20, md:flex-row md:items-end md:justify-between md:px-24. Eyebrow 'AI options desk' in Geist Mono 11px uppercase 0.1em #96969E, mb-6. H1 'Options strategies' at text-display-sm = 40px/1.1, letter-spacing -0.02em, weight 400, #F7F7F8, truncating. Description mt-4, 14px, #D3D3D7: 'Regime-aware, rule-based multi-leg structures for NIFTY / BANKNIFTY / FINNIFTY, scored against live VIX. Ask the AI copilot for a structure, deploy to paper, and watch live mark-to-market P&L.' |
| 2 | **Header actions (right, flex-wrap gap-8)** | Primary Button md (h-32, radius 8, #406AE4 fill, white ink, elev-1): Brain glyph 16px at mr-4 + label 'Ask the AI' — opens the AI-suggest dialog. Ghost Button md: RefreshCw 16px + label 'Refresh' that is hidden below the sm breakpoint (`hidden sm:inline`), aria-label='Refresh' — fires refreshRecs() AND refreshPositions() together. |
| 3 | **Aggregate KPI strip** | section aria-label='Aggregate stats', grid-cols-2 gap-12, md:grid-cols-4. Four StatCards in fixed order: (1) 'Open positions' — plain count; (2) 'Unrealized P&L' — inrSigned, ink #10B981 when ≥0 else #F5808C; (3) 'Realized P&L (last 10 closed)' — inrSigned, same tone rule; (4) 'Max risk exposed' — absolute ₹ magnitude, no sign, plus an ⓘ Info glyph 12px opening a Tooltip: 'Risk-gated view: sum of max_loss across all open positions. Naked positions = unbounded; not counted.' Every tile: radius 8, 1px #29292D, bg #151517, p-16; label Geist Mono 11px uppercase 0.1em #96969E; value Geist Mono 24px weight 400 tabular-nums. |
| 4 | **Market context strip (only when overview.regime exists)** | Card with className 'border-line bg-wrap/60' → CardBody flex-wrap items-center gap-x-24 gap-y-8 py-12, 12px type. Three Meta pairs rendered inline-baseline gap-4: label in Geist Mono 10px uppercase tracking-wider #96969E immediately followed by value in Geist Mono weight 500 #F7F7F8 — 'REGIME sideways', 'VIX 13.42 (falling)', 'VIX 5D MEAN 13.88'. VIX renders as `${current.toFixed(2)} (${direction})` where direction ∈ rising\|falling\|stable. Right-pushed (ml-auto) timestamp in #96969E: 'as of 03 Aug, 03:22 pm' from toLocaleString('en-IN', {day:'2-digit', month:'short', hour:'2-digit', minute:'2-digit'}). |
| 5 | **AI Options Copilot card** | section radius 12, 1px #29292D, bg #151517, p-16. Head row: Sparkles 14px #8FB0FF + h3 'AI Options Copilot' at 12px weight 600; right caption 10px #96969E 'Descriptive — not a trade recommendation.' Row 2 (mt-12, flex-wrap gap-8): 'INDEX' label 10px weight 600 uppercase tracking-wider, then three radius-6 chips NIFTY / BANKNIFTY / FINNIFTY (px-12 py-4, 11px weight 500 — active = .glass-control-accent #406AE4 white ink, inactive = .glass-control #1E1E21 with #D3D3D7 ink), then an ml-auto radius-9999 accent pill with Sparkles: 'Get today's best trade' before first fetch, 'Refresh' afterwards; Loader2 spins in place of Sparkles while loading. NEVER auto-fires — user-triggered only. |
| 6 | **Copilot result body** | Fact chips row (10px #96969E, each radius 6, 1px #29292D, bg #0D0D0E, px-8 py-2): 'VIX · 13.42', 'elevated' (capitalized regime), 'PCR · 0.94', 'Max Pain · 24,200', '3 DTE'. Optional grounded narrative paragraph at 12.5px leading-relaxed #D3D3D7. Then a ul of strategy rows: each li radius 6, p-12 — the head row is 1px #406AE4/60 with bg #406AE4/05 and carries a 'BEST' chip (9px weight 600 uppercase tracking-wider, #8FB0FF ink on accent/10, radius 4); the rest are 1px #29292D on #151517. Row contents: bias glyph (TrendingUp #10B981 / TrendingDown #F5808C / Layers #96969E), name at 14px weight 500, confidence chip (high = 1px #10B981 on up/10; medium = accent; low = muted), right-aligned '~₹142k margin' in Geist Mono 11px; rationale 12px; suggested legs as monospace '• ' bullets 11px; risk notes under a 1px #29292D/60 divider prefixed '⚠ ' at 10px; source_label at 9px opacity 70%. |
| 7 | **Inner tab rail** | TabsList radius 9999, 1px #29292D, bg #0D0D0E, p-4, gap-4, defaultValue='recommendations'. Four triggers px-14 py-6, 14px weight 500: 'Recommendations' + primary Badge count (ml-8), 'Open' + primary Badge count, 'Closed' + muted Badge count, 'Chain' (no badge). Badges are hidden entirely at count 0. Active trigger: bg #151517, ink #F7F7F8, shadow-sm. Each TabsContent is mt-16. |
| 8 | **Footer** | DisclaimerFooter variant='derivatives' — pb-8 pt-4, centered 10px #96969E: 'For educational purposes only. Derivatives carry a high risk of loss and are not suitable for every investor. Not investment advice. Markets carry risk.' |

**Components** — `PageHeader` · `Button (primary / ghost)` · `StatCard ×4` · `Tooltip` · `Card + CardBody` · `Meta (local inline-baseline label/value pair)` · `OptionsCopilotCard` · `Badge (primary / muted / up / down)` · `Tabs + TabsList + TabsTrigger + TabsContent` · `DisclaimerFooter` · `Skeleton`

**States & data.** GET /api/fo-strategies/overview (SWR 'fo:overview', 5min refresh) supplies regime {name, prob_bull, prob_sideways, prob_bear}, vix {current, direction, mean_5d, n_history_days, method}, symbols[], recommendations{}. GET /api/fo-strategies/paper/positions (SWR 'fo:paper:positions', 30s refresh) supplies the whole book. Derived client-side: openPositions = status==='open'; closedPositions = status!=='open' sliced to the first 10; stats.unrealized = Σ unrealized_pnl; stats.maxLoss = Σ max_loss; realizedTotal = Σ realized_pnl over those 10. Loading: all four StatCards show a 60%×28px pulse bar (only while `posLoading && !posResp` — a background revalidate never flickers them). The regime strip is conditionally rendered and simply absent when overview.regime is null — no placeholder.

**Interactions.** 'Ask the AI' opens the AI-suggest dialog. 'Refresh' revalidates BOTH SWR keys at once. Copilot index chips reset the result to idle when switched. Copilot 'Get today's best trade' calls api.screener.fnoBestTrade(symbol, true) and falls back to a quiet 11px line — 'No clear best trade right now — the option chain is unavailable or no rule fired for this regime.' — on empty or error. Inner tabs swap body content only; the header, KPI strip, context strip and copilot card stay mounted.

**Responsive.** KPI strip 2-up below md (768px), 4-up at and above. PageHeader stacks vertically below md and becomes a baseline-aligned row above. The 'Refresh' word disappears below sm (640px), leaving a 16px icon-only ghost button. Context strip wraps freely (gap-x-24 / gap-y-8) and the 'as of' stamp drops to a new line on narrow viewports. Copilot chips wrap; its accent pill keeps ml-auto so it hugs the right edge until wrap.

**Key copy.** 'AI options desk' · 'Options strategies' · 'Regime-aware, rule-based multi-leg structures for NIFTY / BANKNIFTY / FINNIFTY, scored against live VIX. Ask the AI copilot for a structure, deploy to paper, and watch live mark-to-market P&L.' · 'Ask the AI' · 'Refresh' · 'Open positions' · 'Unrealized P&L' · 'Realized P&L (last 10 closed)' · 'Max risk exposed' · 'Risk-gated view: sum of max_loss across all open positions. Naked positions = unbounded; not counted.' · 'AI Options Copilot' · 'Descriptive — not a trade recommendation.' · 'Get today's best trade' · 'For educational purposes only. Derivatives carry a high risk of loss and are not suitable for every investor. Not investment advice. Markets carry risk.'

<details><summary><b>Stitch prompt for this screen</b></summary>

```text
Design a dark-mode Indian derivatives trading workspace header on a #0D0D0E canvas. Section header: Geist Mono 11px uppercase letter-spaced label 'AI OPTIONS DESK' in #96969E, then a 40px weight-400 white (#F7F7F8) headline 'Options strategies', then a 14px #D3D3D7 subline 'Regime-aware, rule-based multi-leg structures for NIFTY / BANKNIFTY / FINNIFTY, scored against live VIX.' Right side: a solid #406AE4 pill button with white text 'Ask the AI' and a brain glyph, plus a transparent ghost button 'Refresh' with a refresh glyph in #D3D3D7. A 1px #29292D rule closes the header. Beneath it, a 4-column KPI row of 8px-radius cards, each #151517 with a 1px #29292D border and 16px padding: 'OPEN POSITIONS' 3, 'UNREALIZED P&L' +₹18,420 in #10B981, 'REALIZED P&L (LAST 10 CLOSED)' −₹6,150 in #F5808C, 'MAX RISK EXPOSED' ₹1,42,500 with a small info icon. Labels are Geist Mono 11px uppercase #96969E; values are Geist Mono 24px tabular figures. Below, a slim 12px-radius context bar reading 'REGIME sideways   VIX 13.42 (falling)   VIX 5D MEAN 13.88' with 'as of 03 Aug, 03:22 pm' right-aligned in #96969E. Finally a pill tab rail with rounded 9999px tabs: Recommendations 6, Open 3, Closed 10, Chain — active tab filled #151517. Restrained, instrument-like, no gradients, no glow.
```
</details>

---

## `/fno?tab=lab#recommendations` — Recommendations grid — live rule-based multi-leg proposals

**File** `/Users/rishi/QuantX/frontend/components/fno/FoStrategiesWorkspace.tsx (TabsContent L374-400; RecommendationCard L1047-1127)` · **108 LOC** · **Access** Authenticated. Content is public-to-the-user rule output — no broker link required (premiums fall back to model estimates when the chain is unavailable).

**Shell** — Inside the workspace shell, TabsContent value='recommendations' className='mt-4' (16px above the grid).

**Purpose.** Default inner tab. Flattens overview.recommendations (a Record<symbol, proposal[]>) into one two-column card grid where every card is a fully priced, deployable multi-leg structure with its view, economics, legs and two actions.

### Anatomy (render order)

| # | Region | What renders |
|---|---|---|
| 1 | **Grid** | grid-cols-1 gap-16 md:grid-cols-2. Cards are equal-width, natural-height; no masonry, no virtualization. React key = `${symbol}-${strategy}-${index}`. |
| 2 | **Card container** | Foundation Card — radius 12, 1px #29292D, bg #151517, shadow-none, density default so header/body pad to 20px horizontal / 16px vertical. |
| 3 | **CardHeader** | min-h 44px, flex items-center justify-between gap-8, border-b 1px #29292D. LEFT (min-w-0, gap-8): a primary-tone Badge in UPPERCASE carrying the symbol — 'NIFTY' / 'BANKNIFTY' / 'FINNIFTY' — (radius 9999, px-10 py-2, bg #406AE4/10, ink #8FB0FF, border #406AE4/20) followed by the strategy title, 14px weight 600 #F7F7F8, truncating. RIGHT: a credit/debit Badge in UPPERCASE — tone 'up' (#10B981 on up/10) when credit_debit==='credit', tone 'down' (#F5808C on down/10) when 'debit'. Literal text is the lowercase enum uppercased by CSS: 'CREDIT' / 'DEBIT'. |
| 4 | **View line** | First child of CardBody (space-y-12): the proposal's plain-English view at 12px #D3D3D7, e.g. 'Range-bound tape with elevated IV — sell the wings, keep risk defined.' |
| 5 | **Economics triplet** | grid-cols-3 gap-8 at 11px. Three KPI wells, each radius 12, 1px #29292D, bg #151517/60, p-8, text-center: label in Geist Mono 9px uppercase tracking-wider #96969E, value mt-2 in Geist Mono 12px weight 600 tabular-nums. (1) 'MAX PROFIT' tone up (#10B981) — inrSigned. (2) 'MAX LOSS' tone down (#F5808C) — inrSigned of the negated magnitude, or the literal string 'Unbounded' when max_loss is null. (3) 'P(PROFIT)' neutral ink (#F7F7F8) — `${(probability_of_profit*100).toFixed(0)}%`, or '—' when null. |
| 6 | **Legs well** | radius 12, 1px #29292D, bg #151517/60, p-8. Caption 'LEGS' in Geist Mono 9px uppercase tracking-wider #96969E, mb-4. Then a ul space-y-4 in Geist Mono 11px; every li is flex justify-between gap-8 — left is the action word colored (#10B981 for BUY, #F5808C for SELL) followed by option_type and strike ('SELL CE 24300'), right is '@ ₹142.65' in #96969E (premium?.toFixed(2), '—' when absent). |
| 7 | **Action footer** | flex items-center justify-between gap-8. LEFT caption at 10px #96969E: 'Expiry 2026-08-06 · Net premium +₹8,240' (raw expiry string, inrSigned net premium). RIGHT (flex gap-4): ghost Button size sm (h-28, radius 8, px-12, 12px weight 600, ink #D3D3D7) with BarChart3 14px + 'Backtest'; then primary Button size sm (#406AE4 fill, white ink) with Radio 14px + 'Deploy' + a trailing ChevronRight 14px at ml-2. |

**Components** — `Card` · `CardHeader` · `CardBody` · `Badge (primary + up/down)` · `KPI (local well: label + tabular value)` · `Button sm ghost` · `Button sm primary` · `Skeleton` · `EmptyState` · `lucide: BarChart3, Radio, ChevronRight, Target`

**States & data.** LOADING (`recsLoading && !overview`): the exact same 1/2-column grid filled with four Skeletons at w='100%' h='240px' — so the skeleton height is a hard 240px. EMPTY (allRecs.length === 0): EmptyState with a 32px Target glyph, title 'No structures flagged right now', description 'The engine ranks multi-leg structures per index symbol from the current regime + VIX slope. In a transition tape none may clear. Check back at the next 9:30 IST refresh.' POPULATED: one card per proposal across all symbols. Fields consumed per card: symbol, strategy, name, view, legs[{action, option_type, strike, premium}], max_profit, max_loss, breakevens, net_premium, credit_debit, lot_size, probability_of_profit, expiry, strike_interval. NOTE: breakevens and strike_interval are present in the payload but NOT rendered on the card. Refresh cadence 5 minutes, deduped at 60s, never on focus.

**Interactions.** 'Deploy' sets deployFor=<this proposal> and resets lots to 1, which opens the Deploy-confirm dialog. 'Backtest' sets backtestFor, clears prior result/error, sets backtestLoading and immediately POSTs /api/fo-strategies/backtest with {template: strategy, symbol, lookback_days: 180, initial_capital: 100000} — the modal opens instantly in its loading state. Cards themselves are not clickable; there is no hover lift (elevation flush, interactive false).

**Responsive.** Single column below md (768px), two columns at and above, 16px gutter. Card header symbol badge + title share min-w-0 so long strategy names truncate rather than pushing the credit/debit badge off-row. The economics triplet stays 3-up at every width. The action footer can wrap its caption above the buttons on very narrow viewports.

**Key copy.** 'No structures flagged right now' · 'The engine ranks multi-leg structures per index symbol from the current regime + VIX slope. In a transition tape none may clear. Check back at the next 9:30 IST refresh.' · 'MAX PROFIT' / 'MAX LOSS' / 'P(PROFIT)' / 'LEGS' · 'Unbounded' · 'Expiry … · Net premium …' · 'Backtest' · 'Deploy'

<details><summary><b>Stitch prompt for this screen</b></summary>

```text
Design a two-column grid of dark options-strategy cards for an Indian NSE trading app on a #0D0D0E canvas, 16px gutters. Each card: 12px radius, #151517 fill, 1px #29292D border, no shadow. Header row (44px tall, 20px side padding, 1px #29292D bottom rule): a pill badge 'NIFTY' in #8FB0FF on a 10%-opacity #406AE4 tint, then a 14px semibold white title 'Iron Condor'; far right a pill badge 'CREDIT' in #10B981 on a green tint. Body: a 12px #D3D3D7 sentence 'Range-bound tape with elevated IV — sell the wings, keep risk defined.' Then three equal centered wells (12px radius, #29292D hairline): 'MAX PROFIT' ₹8,240 in #10B981, 'MAX LOSS' −₹16,760 in #F5808C, 'P(PROFIT)' 68%. Labels are Geist Mono 9px uppercase letter-spaced #96969E; values Geist Mono 12px semibold tabular. Below, a 'LEGS' well listing four monospace 11px rows, each with a colored verb and a right-aligned price: 'SELL CE 24300 @ ₹142.65', 'BUY CE 24500 @ ₹64.10', 'SELL PE 23900 @ ₹128.40', 'BUY PE 23700 @ ₹58.25' — BUY in #10B981, SELL in #F5808C. Footer: 10px #96969E 'Expiry 2026-08-06 · Net premium +₹8,240' at left; at right a small transparent 'Backtest' button and a solid #406AE4 'Deploy ›' button with white text, both 28px tall with 8px radius. Show a second card 'Bull Call Spread / BANKNIFTY / DEBIT'.
```
</details>

---

## `/fno?tab=lab#deploy-confirm` — Deploy to paper — confirmation dialog

**File** `/Users/rishi/QuantX/frontend/components/fno/FoStrategiesWorkspace.tsx (Dialog L449-533; handleDeploy L158-179; SmallMeta L1290-1299)` · **85 LOC** · **Access** Authenticated. Opened only from a RecommendationCard's Deploy button (deployFor !== null).

**Shell** — Radix portal — overlay fixed inset-0 z-40 rgba(0,0,0,.60) with animate-overlay-in; content fixed left-1/2 top-1/2 z-50, translated -50%/-50%, scaling in from 0.97.

**Purpose.** The single sizing gate between a recommendation and an open paper position. Restates the structure's economics, takes a lot count (1-20, clamped), previews the per-leg share count and total cash flow, then commits.

### Anatomy (render order)

| # | Region | What renders |
|---|---|---|
| 1 | **Panel** | w-full !max-w-md → 448px. Radius 8, 1px #29292D, bg #151517, p-20, shadow elev-1. Title (Radix Title, mb-8, 16px weight 400 #F7F7F8): `Deploy Iron Condor — NIFTY` — literally `Deploy ${STRATEGY_LABEL[strategy] ?? strategy} — ${symbol}` with an EM DASH separator. A visually-hidden Radix Description mirrors the title for screen readers. |
| 2 | **View restatement** | First row of the space-y-12 body: the proposal's `view` string at 12px #D3D3D7. |
| 3 | **Economics grid** | grid-cols-2 gap-8 at 12px — four SmallMeta wells (radius 12, 1px #29292D, bg #151517/60, p-8; label Geist Mono 9px uppercase tracking-wider #96969E; value mt-2 Geist Mono 12px weight 500 tabular-nums #F7F7F8). Reading order: 'NET PREMIUM' (inrSigned); then a label that SWITCHES on credit_debit — 'MAX PROFIT (CREDIT)' when credit, plain 'MAX PROFIT' when debit; then 'MAX LOSS' (negated magnitude, or the literal 'Unbounded'); then 'P(PROFIT)' as a whole-number percent or '—'. |
| 4 | **Lots field** | space-y-4 block. Label htmlFor='lots-input', 12px weight 500 #F7F7F8: `Deployment lots (×75 per lot)` — the lot_size is interpolated from the proposal. NumericInput id='lots-input' min=1 max=20; onChange hard-clamps with Math.max(1, Math.min(20, Number(v) \|\| 1)) so a cleared field snaps back to 1. Below it a Geist Mono 10px #96969E preview line: `300 shares per leg · cash out ≈ −₹32,960` — the noun flips between 'out' (debit) and 'in' (credit), and the amount is inrSigned(net_premium × lots). |
| 5 | **Action bar** | flex justify-end gap-8, border-t 1px #29292D, pt-12. Ghost Button 'Cancel' (disabled while deploying) which also resets lots to 1. Then a hand-rolled pill — NOT the Button primitive: class 'glass-control-accent inline-flex h-9 items-center gap-1.5 rounded-full px-4 text-[13px] font-semibold' → 36px tall, radius 9999, #406AE4 fill, white ink, elev-1, 16px horizontal padding, 13px/600 label, active:scale(0.97). Content: Radio glyph 16px (replaced by a 14px spinning Loader2 while deploying) + 'Deploy to paper'. Disabled when deploying or lots < 1. |

**Components** — `Dialog (Radix)` · `SmallMeta ×4` · `NumericInput` · `Button ghost` · `raw .glass-control-accent pill button` · `lucide: Radio, Loader2` · `toast.success / toast.error`

**States & data.** Open state is driven by `deployFor` (a FoStrategyProposal) — the whole body is conditionally rendered so the panel is empty for one frame on close. `lots` state defaults to 1 and is re-seeded to 1 every time a card's Deploy is pressed. `deploying` gates the submit button, the Cancel button AND the dismiss path — onClose returns early while deploying, so neither Escape nor the overlay can close an in-flight deploy. POST /api/fo-strategies/paper/open with {template: strategy, symbol, lots}. Response supplies position_id, trade_id, net_premium, max_profit, max_loss, legs[].

**Interactions.** Success → toast.success titled `Deployed Iron Condor on NIFTY` with description `Net premium: +₹8,240 · 2 lot(s)`, then deployFor=null, lots=1, and refreshPositions() — so the Open tab badge increments within the same tick. Failure → toast.error 'Deploy failed' with the handleApiError string as description; the dialog STAYS OPEN so the user can retry. Cancel and overlay-click both clear deployFor and reset lots.

**Responsive.** Fixed 448px cap, w-full below that, so on a 360px phone it becomes a full-bleed centered card with the page's own margin. The 2-column economics grid never collapses. The action bar stays right-aligned and can wrap the two buttons on very narrow screens.

**Key copy.** 'Deploy Iron Condor — NIFTY' · 'NET PREMIUM' · 'MAX PROFIT (CREDIT)' · 'MAX LOSS' · 'P(PROFIT)' · 'Unbounded' · 'Deployment lots (×75 per lot)' · '300 shares per leg · cash out ≈ −₹32,960' · 'Cancel' · 'Deploy to paper' · toast: 'Deployed Iron Condor on NIFTY' / 'Net premium: +₹8,240 · 2 lot(s)' / 'Deploy failed'

<details><summary><b>Stitch prompt for this screen</b></summary>

```text
Design a 448px-wide dark confirmation modal centered over a 60%-black scrim, for deploying an options structure to a paper account. Panel: 8px radius, #151517 fill, 1px #29292D border, 20px padding, soft shadow. Title in 16px regular white: 'Deploy Iron Condor — NIFTY'. Under it a 12px #D3D3D7 line: 'Range-bound tape with elevated IV — sell the wings, keep risk defined.' Then a 2×2 grid of small wells (12px radius, #29292D hairline, subtle #151517 fill, 8px padding). Each well: a Geist Mono 9px uppercase letter-spaced #96969E label above a Geist Mono 12px medium tabular value — 'NET PREMIUM' +₹8,240, 'MAX PROFIT (CREDIT)' +₹8,240, 'MAX LOSS' −₹16,760, 'P(PROFIT)' 68%. Below, a 12px medium white label 'Deployment lots (×75 per lot)' with a numeric stepper field: 8px radius, #1E1E21 fill, 1px #29292D border, 40px tall, value 2, with a narrow right-hand column holding stacked + and − glyphs divided by a hairline. Under the field a Geist Mono 10px #96969E note: '150 shares per leg · cash in ≈ +₹16,480'. Close with a 1px #29292D rule and a right-aligned action row: a transparent 'Cancel' button in #D3D3D7 and a solid 36px-tall fully-rounded #406AE4 pill with white 13px semibold text 'Deploy to paper' and a small broadcast glyph. Precise, restrained, no gradients.
```
</details>

---

## `/fno?tab=lab#open` — Open paper positions — live 30s mark-to-market book

**File** `/Users/rishi/QuantX/frontend/components/fno/FoStrategiesWorkspace.tsx (TabsContent L402-424; OpenPositionCard L1129-1223; Cell L1267-1279)` · **118 LOC** · **Access** Authenticated. Empty for users who have never deployed.

**Shell** — Inside the workspace shell, TabsContent value='open' className='mt-4'; cards in a vertical stack with space-y-12.

**Purpose.** The live book. One card per open multi-leg paper position showing four marked-to-market cells and a full per-leg table with entry→LTP drift, plus the two per-position actions (Adjust, Close). This is the only surface in the workspace that re-renders on a 30-second clock.

### Anatomy (render order)

| # | Region | What renders |
|---|---|---|
| 1 | **Card + header** | Foundation Card (radius 12, 1px #29292D, bg #151517). CardHeader min-h 44px, flex justify-between gap-8, border-b 1px #29292D, px-20 py-16. LEFT (gap-8): primary Badge UPPERCASE = underlying ('NIFTY'); strategy title 14px weight 600 #F7F7F8 resolved through STRATEGY_LABEL with fallbacks template_slug → the literal string 'Multi-leg'; then, VISIBLE ONLY AT md AND ABOVE (`hidden … md:inline`), a Geist Mono 10px #96969E stamp 'exp 2026-08-06'. RIGHT (flex gap-6): the Adjust button then the Close button, both ghost size sm. |
| 2 | **MTM cell grid** | CardBody has p-0 — the body's own padding is removed so the cell grid bleeds edge-to-edge. Grid is `grid-cols-2 gap-px bg-line/40 sm:grid-cols-4`: the 1px gap over a #29292D/40 background IS the rule system — each cell paints bg #151517 over it, producing hairline separators with no borders. Four cells, each p-12: (1) 'UNREALISED P&L' — an inline-flex gap-4 group with a 12px TrendingUp when pnl ≥ 0 or TrendingDown when < 0, tinted #10B981 / #F5808C / #F7F7F8 at exactly zero, value inrSigned; (2) 'NET PREMIUM (ENTRY)' inrSigned; (3) 'CURRENT VALUE' inrSigned; (4) 'MAX LOSS' tone down, negated magnitude or the literal 'Unbounded'. Cell label Geist Mono 9px uppercase tracking-wider #96969E; value mt-2 Geist Mono 14px weight 600 tabular-nums. |
| 3 | **Per-leg table** | mx-16 mb-12 (inset from the card edges because the body is unpadded), overflow-hidden, radius 12, 1px #29292D. HEADER: grid-cols-12 gap-8, border-b 1px #29292D, bg #151517/60, px-12 py-6, Geist Mono 9px uppercase tracking-wider #96969E — exact spans and alignments: 'Side' col-span-2 (left), 'Type' col-span-2 (left), 'Strike' col-span-3 (RIGHT), 'Lots' col-span-2 (RIGHT), 'Entry → LTP' col-span-3 (RIGHT). ROWS: grid-cols-12 items-center gap-8, border-b 1px #29292D/60 with last:border-b-0, px-12 py-6, Geist Mono 11px. Side is tinted #10B981 for BUY / #F5808C for SELL; Type is plain; Strike and Lots are right-aligned tabular-nums; the last column is right-aligned tabular-nums in #D3D3D7 rendering '₹142.65 → ₹118.30', with '—' after the arrow when current_price is null. |
| 4 | **Row actions** | Adjust: ghost Button sm, Wrench 14px at mr-4, label 'Adjust', aria-label='Suggest adjustments' — see the Adjust screen. Close: ghost Button sm, XCircle 14px at mr-4 (swapped for a 14px spinning Loader2 while that specific position is closing), label 'Close', aria-label='Close position', disabled while closing. |

**Components** — `Card` · `CardHeader` · `CardBody (p-0)` · `Badge primary` · `Cell ×4 (local hairline-gap grid)` · `AdjustmentButton` · `Button ghost sm` · `Skeleton` · `EmptyState` · `lucide: TrendingUp, TrendingDown, XCircle, Loader2, Wrench, Radio`

**States & data.** LOADING (`posLoading && !posResp`): a single Skeleton w='100%' h='200px' — not a card-shaped placeholder. EMPTY: EmptyState with a 32px Radio glyph, title 'No open paper positions', description 'Pick a flagged structure above and click Deploy to paper to open a multi-leg position. The book marks to market live, refreshed every 30 seconds.' POPULATED: positions filtered to status==='open', keyed by id. Per-position fields consumed: id, underlying, template_slug, expiry_date, net_premium, max_profit, max_loss, current_value, unrealized_pnl, status, legs[{id, side, option_type, strike, expiry_date, lots, lot_size, entry_price, current_price, exit_price}]. NOTE: max_profit, entry_at, last_marked_at and metadata come down the wire but are NOT rendered on this card. SWR refreshInterval 30000ms with keepPreviousData, so values update in place without a skeleton flash.

**Interactions.** Adjust opens the hand-rolled Defend & adjust overlay AND fires its POST in the same click handler. Close fires the close request immediately — there is no confirm step. Only the clicked card is disabled (closing state is tracked by a single `closingId` string, compared per card). Cards are not clickable, do not lift, and have no expand/collapse.

**Responsive.** MTM cells are 2-up below sm (640px) and 4-up at and above — the hairline gap grid reflows automatically. The 'exp <date>' stamp in the header is hidden below md (768px). The per-leg table is a 12-column CSS grid, not a scroller: at narrow widths the columns compress rather than overflow. Cards are always full width, stacked with 12px gaps.

**Key copy.** 'No open paper positions' · 'Pick a flagged structure above and click Deploy to paper to open a multi-leg position. The book marks to market live, refreshed every 30 seconds.' · 'UNREALISED P&L' (British spelling here, vs 'Unrealized P&L' in the KPI strip above — as built) · 'NET PREMIUM (ENTRY)' · 'CURRENT VALUE' · 'MAX LOSS' · 'Unbounded' · column heads 'Side / Type / Strike / Lots / Entry → LTP' · 'Adjust' · 'Close' · 'Multi-leg'

<details><summary><b>Stitch prompt for this screen</b></summary>

```text
Design a dark live options-position card for an Indian NSE paper-trading book on a #0D0D0E canvas. Card: 12px radius, #151517, 1px #29292D border. Header (44px, 20px padding, bottom hairline): pill badge 'BANKNIFTY' in #8FB0FF on a 10% #406AE4 tint, a 14px semibold white title 'Short Strangle', and a Geist Mono 10px #96969E stamp 'exp 2026-08-06'; at the right two small transparent buttons with 14px glyphs — a wrench labeled 'Adjust' and an x-circle labeled 'Close', both text #D3D3D7. Directly under the header, a full-bleed 4-cell strip separated by 1px #29292D hairlines, each cell 12px padding on #151517: 'UNREALISED P&L' +₹18,420 in #10B981 with a small up-trend arrow, 'NET PREMIUM (ENTRY)' +₹26,800, 'CURRENT VALUE' +₹8,380, 'MAX LOSS' Unbounded in #F5808C. Labels Geist Mono 9px uppercase letter-spaced #96969E, values Geist Mono 14px semibold tabular. Below, inset 16px from the card edges, a 12px-radius bordered mini table. Header row on a slightly lighter fill, Geist Mono 9px uppercase #96969E: Side, Type, right-aligned Strike, right-aligned Lots, right-aligned 'Entry → LTP'. Four monospace 11px rows on hairline dividers: 'SELL CE 52800 · 2 · ₹186.40 → ₹142.15' and 'SELL PE 51200 · 2 · ₹171.25 → ₹198.60' — BUY in #10B981, SELL in #F5808C, numbers tabular.
```
</details>

---

## `/fno?tab=lab#close` — Close position flow — immediate, no confirm dialog

**File** `/Users/rishi/QuantX/frontend/components/fno/FoStrategiesWorkspace.tsx (handleClose L246-259; Close button L1158-1171; closingId state L102)` · **32 LOC** · **Access** Authenticated; only reachable from a card the user owns.

**Shell** — No surface of its own. It is the disabled/spinner state of one OpenPositionCard's Close button, plus a global Sonner toast, plus the resulting cross-tab data move.

**Purpose.** Squares off an open multi-leg paper position at current marks and books realised P&L. Documented separately because it is a distinct interaction state of the Open card — and because the as-built flow deliberately has NO confirmation dialog.

### Anatomy (render order)

| # | Region | What renders |
|---|---|---|
| 1 | **Trigger** | Ghost Button size sm inside the OpenPositionCard header: h-28, radius 8, gap-6, px-12, 12px weight 600, ink #D3D3D7, hover bg #1E1E21 with ink #F7F7F8. Idle content: XCircle 14px at mr-4 + 'Close'. aria-label='Close position'. |
| 2 | **In-flight state** | The XCircle is REPLACED (not overlaid) by a 14px Loader2 spinning at mr-4; the label 'Close' is unchanged; the button is disabled (cursor-not-allowed, opacity 50%, active:scale suppressed). Because state is a single `closingId` string compared against p.id, only the clicked card enters this state — every other card's Close stays live. |
| 3 | **No confirm step** | AS BUILT: the click handler calls api.foStrategies.paperClose(id) directly. There is no Dialog, no ConfirmDialog, no are-you-sure copy, no typed confirmation, and no undo. The only affordance between intent and execution is the button's own hit target. |
| 4 | **Success feedback** | Sonner toast.success titled 'Position closed' with description `Realised +₹8,240 (+31.2%)` — built from inrSigned(realized_pnl) and a hand-rolled percent formatter that always prefixes '+' for non-negative values and uses toFixed(1). |
| 5 | **Failure feedback** | toast.error titled 'Close failed' with the handleApiError string as description. The card returns to idle and remains open — nothing is optimistically removed. |
| 6 | **Post-close data move** | refreshPositions() re-fetches the whole book. The position's status flips away from 'open', so it disappears from the Open list, the 'Open' tab Badge count decrements, the 'Open positions' and 'Unrealized P&L' KPI tiles recompute, and the row appears at the top of the Closed tab (which slices the first 10 non-open positions) with its exit_reason and realised P&L. |

**Components** — `Button ghost sm` · `lucide: XCircle, Loader2` · `toast.success / toast.error (Sonner)` · `SWR mutate (refreshPositions)`

**States & data.** POST /api/fo-strategies/paper/{position_id}/close. Response: {success, position_id, realized_pnl, realized_pnl_pct}. closingId is set before the request and cleared in a finally block, so the spinner always resolves. No optimistic mutation — the UI waits for the server and then revalidates.

**Interactions.** Single click executes. Repeat clicks are impossible while in flight (button disabled). No keyboard confirm path exists beyond activating the button. The 30s MTM poll continues during the close and may repaint other cards' numbers mid-flight.

**Responsive.** Inherits the OpenPositionCard header row; on narrow viewports the Adjust + Close pair wraps under the badge/title group. Toasts render in the global Sonner region (top-right on desktop, full-width on mobile).

**Key copy.** 'Close' · aria-label 'Close position' · toast title 'Position closed' · toast description 'Realised +₹8,240 (+31.2%)' · toast title 'Close failed'

<details><summary><b>Stitch prompt for this screen</b></summary>

```text
Design three side-by-side states of a small destructive-adjacent action button inside a dark trading card, plus its success toast, on a #0D0D0E background. STATE 1 idle: a 28px-tall transparent button with 8px radius, 12px semibold #D3D3D7 label 'Close' preceded by a 14px x-circle glyph. STATE 2 hover: same button with a #1E1E21 fill and #F7F7F8 label. STATE 3 in-flight: the x-circle replaced by a 14px circular spinner, the label still 'Close', the whole button at 50% opacity with a not-allowed cursor. Place all three inside a fragment of an options position card — 12px radius, #151517, 1px #29292D border — whose header also shows a 'NIFTY' pill badge in #8FB0FF on a #406AE4 10% tint, a 14px semibold white title 'Iron Condor', and a wrench button labeled 'Adjust'. To the right, render the resulting toast: a compact 8px-radius #151517 card with a 1px #29292D border and a green #10B981 check glyph, a 14px white title 'Position closed', and a 12px #96969E description 'Realised +₹8,240 (+31.2%)' with tabular figures. Below it a second toast variant with a #F5808C x glyph titled 'Close failed'. Emphasise that no confirmation dialog appears — the click executes immediately.
```
</details>

---

## `/fno?tab=lab#adjust` — Defend & adjust — ranked roll/hedge/defend suggestions overlay

**File** `/Users/rishi/QuantX/frontend/components/fno/FoStrategiesWorkspace.tsx (AdjustmentButton L2417-2558)` · **142 LOC** · **Access** Authenticated for the position context; the adjustments endpoint itself is called with auth:false.

**Shell** — A hand-rolled overlay, NOT the Radix Dialog primitive: `fixed inset-0 z-50 flex items-end justify-center bg-black/70 p-4 md:items-center` — so it is a BOTTOM SHEET on mobile and a CENTERED MODAL from md up. Clicking the scrim closes it unless a request is in flight.

**Purpose.** Per-position adjustment engine. POSTs the position's economics, its legs and a derived spot to the rule engine and renders ranked defend/roll/hedge/close/scale-in playbooks with numbered steps and risk notes. Reachable from every open position card.

### Anatomy (render order)

| # | Region | What renders |
|---|---|---|
| 1 | **Trigger** | Ghost Button size sm in the OpenPositionCard header: Wrench 14px at mr-4 + 'Adjust', aria-label='Suggest adjustments'. onClick does two things at once — setOpen(true) and loadAdjustments() — so the overlay always opens directly into its loading state. |
| 2 | **Panel** | w-full max-w-2xl (672px), max-h-[80vh] with overflow-y-auto, radius 16 (rounded-lg — the modal/sheet step), 1px #29292D, bg #0D0D0E (the PAGE canvas colour, not the card colour — this panel is deliberately darker than the cards behind it), p-20, shadow-2xl. Click inside is stopPropagation'd so it never dismisses. |
| 3 | **Panel header** | flex items-center justify-between, border-b 1px #29292D, pb-12. LEFT gap-8: Wrench 20px in #8FB0FF + h3 'Defend & adjust' at 16px weight 600 #F7F7F8 (the ampersand is HTML-escaped in source). RIGHT: a bare icon button, XCircle 16px, #96969E → #F7F7F8 on hover, aria-label='Close'. |
| 4 | **Loading** | space-y-8 py-16 containing exactly three Skeletons at w='100%' h='60px' — a 3-row placeholder that matches the typical suggestion count. |
| 5 | **Error** | A single p: radius 12, 1px #F5808C/30, bg #F5808C/10, px-12 py-8, 14px, ink #F5808C. Carries the raw handleApiError message. |
| 6 | **Empty** | p py-24, centered, 14px #96969E: 'Position doesn't match any adjustment rule right now — hold per your original thesis.' |
| 7 | **Suggestion list** | ul mt-12 space-y-8. Each li: radius 12, 1px #29292D, bg #151517, p-12. TOP ROW flex-wrap items-baseline justify-between gap-8 — left group gap-8 holds the suggestion name at 14px weight 500 #F7F7F8 followed by an URGENCY CHIP (radius 9999, 1px border, px-8 py-2, 10px, capitalize): 'critical' = 1px #F5808C border, bg #F5808C/10, ink #F5808C; 'recommended' = 1px #406AE4/60, bg #406AE4/05, ink #8FB0FF; anything else ('optional') = 1px #29292D, bg #0D0D0E, ink #96969E. Right of that row sits the ACTION verb at 10px uppercase tracking-wider #96969E — one of roll / hedge / defend / close / scale_in. |
| 8 | **Suggestion body** | Rationale p mt-4 at 12px #D3D3D7. Steps as an ordered list: ol mt-8, list-decimal, space-y-2, pl-20, 11px #D3D3D7 — real numbered steps, not bullets. Risk notes as a ul mt-8 space-y-2 sitting above a border-t 1px #29292D/60 with pt-8, at 10px #96969E, each item literally prefixed with the character '⚠ '. Finally a provenance line p mt-4 at 9px #96969E with opacity 70% carrying source_label. |
| 9 | **Panel footer** | p mt-16, border-t 1px #29292D, pt-12, 10px #96969E: 'Adjustments are rule-based suggestions, not auto-executed. Review and place orders manually via your broker.' |

**Components** — `Button ghost sm (trigger)` · `raw fixed overlay (not Radix)` · `Skeleton ×3` · `lucide: Wrench, XCircle` · `ordered/unordered lists` · `urgency chip (inline, three tone branches)`

**States & data.** POST /api/screener/fno/adjustments with {position: {net_premium, unrealized_pnl, expiry_date}, legs: [{side, option_type, strike, lots, lot_size}], spot, vix: null}. Response {count, adjustments: [{action, name, urgency, rationale, steps[], expected_outcome, risk_notes[], source_label}]}. IMPORTANT AS-BUILT DETAIL: `spot` is a UI-side proxy — it is taken as the strike of the MIDDLE leg (legs[Math.floor(legs.length/2)].strike), with 0 when there are no legs; the code comments call this a UI-only proxy with the backend authoritative. `vix` is always sent as null from this call site. NOTE: `expected_outcome` is returned but is NOT rendered anywhere in the list item.

**Interactions.** Opening always refetches (no cache). Scrim click closes only when not loading. The X button closes unconditionally. There is no retry button on error — the user must close and press Adjust again. Nothing in this overlay places an order; it is advisory only, which the footer states explicitly. State is local to each AdjustmentButton, so two positions can be inspected in sequence without cross-talk.

**Responsive.** Below md (768px) the flex container uses items-end so the panel docks to the BOTTOM of the viewport like a sheet, with 16px inset; at md and above items-center centers it. Width is w-full up to a 672px cap. The panel scrolls internally at 80vh. Suggestion top rows wrap (flex-wrap items-baseline) so a long name pushes the action verb to a second line rather than truncating.

**Key copy.** 'Adjust' · aria-label 'Suggest adjustments' · 'Defend & adjust' · 'Position doesn't match any adjustment rule right now — hold per your original thesis.' · '⚠ ' risk-note prefix · 'Adjustments are rule-based suggestions, not auto-executed. Review and place orders manually via your broker.'

<details><summary><b>Stitch prompt for this screen</b></summary>

```text
Design a 672px-wide dark advisory modal over a 70%-black scrim for an Indian options desk; on mobile it docks to the bottom as a sheet. Panel: 16px radius, #0D0D0E fill, 1px #29292D border, 20px padding, heavy shadow, internally scrollable. Header above a hairline: a 20px wrench glyph in #8FB0FF beside a 16px semibold white heading 'Defend & adjust', with a small #96969E x-circle at the far right. Body: three suggestion cards, each 12px radius, #151517 fill, 1px #29292D border, 12px padding. Card one: 14px medium white title 'Roll the tested short call up and out' with a fully-rounded chip 'critical' in #F5808C on a red tint with a #F5808C border, and at the far right a 10px uppercase letter-spaced #96969E word 'roll'. Below, a 12px #D3D3D7 rationale 'NIFTY 24,180 has breached your 24,100 short strike with 3 days to expiry.' Then a numbered list at 11px #D3D3D7: '1. Buy back the 24100 CE at ₹142.65  2. Sell the 24400 CE of the 2026-08-13 expiry  3. Keep total lots at 2'. Then, above a faint hairline, two 10px #96969E lines each starting with a warning triangle: '⚠ Rolling out extends duration risk' and '⚠ Net credit may turn to debit'. Card two uses a blue 'recommended' chip in #8FB0FF on a #406AE4 tint with the verb 'hedge'; card three a neutral grey 'optional' chip with 'scale_in'. Footer line in 10px #96969E: 'Adjustments are rule-based suggestions, not auto-executed.'
```
</details>

---

## `/fno?tab=lab#closed` — Closed book — last 10 realised positions, headerless audit rows

**File** `/Users/rishi/QuantX/frontend/components/fno/FoStrategiesWorkspace.tsx (TabsContent L426-441; ClosedPositionRow L1225-1251)` · **43 LOC** · **Access** Authenticated.

**Shell** — Inside the workspace shell, TabsContent value='closed' className='mt-4'; rows stacked with space-y-8 (8px, tighter than the Open tab's 12px).

**Purpose.** A compact audit trail of the ten most recent non-open positions with realised P&L, exit reason and close timestamp. Deliberately the flattest surface in the workspace — no cards, no actions, no drill-in.

### Anatomy (render order)

| # | Region | What renders |
|---|---|---|
| 1 | **Row container** | Not a Card — a bare div: grid grid-cols-12 items-center gap-8, radius 12, 1px #29292D, bg #151517/40 (a translucent knock-back of the card colour, quieter than an Open card), px-12 py-8, 12px type. No hover state, no cursor change, no click target. |
| 2 | **Column 1 (col-span-3)** | Geist Mono, ink #F7F7F8: `{underlying} · {Strategy Label}` — e.g. 'NIFTY · Iron Condor'. Falls back to the raw template_slug when the label map misses. |
| 3 | **Column 2 (col-span-2)** | Geist Mono #96969E: the exit_reason string as returned (e.g. 'target_hit', 'manual', 'expired'), or '—' when null. |
| 4 | **Column 3 (col-span-3)** | Geist Mono #96969E: closed_at via toLocaleString('en-IN', {day:'2-digit', month:'short', hour:'2-digit', minute:'2-digit'}) → '02 Aug, 03:20 pm'. '—' when null. |
| 5 | **Column 4 (col-span-2, right)** | Right-aligned Geist Mono tabular-nums realised P&L via inrSigned, tinted #10B981 above zero, #F5808C below zero, #F7F7F8 at exactly zero. |
| 6 | **Column 5 (col-span-2, right)** | Right-aligned Geist Mono 10px #96969E: `entry +₹8,240` — the literal lowercase word 'entry' followed by inrSigned(net_premium). |
| 7 | **Missing chrome (as built)** | There is NO column header row on this table, no sort control, no pagination, no 'view all' link and no per-row action. The 12-column grid is implied by alignment alone. Total column spans sum to 12 exactly (3+2+3+2+2). |

**Components** — `ClosedPositionRow (local, no Card)` · `EmptyState` · `lucide: CheckCircle2`

**States & data.** Derived entirely client-side from the same positions payload: `positions.filter(p => p.status !== 'open').slice(0, 10)` — so both 'closed' and 'expired' statuses land here, capped at ten, in whatever order the API returned. EMPTY: EmptyState with a 32px CheckCircle2 glyph, title 'No closed positions yet', description 'Closed positions appear here with realised P&L + exit reason.' There is no separate loading state — the tab simply shows the empty state until data arrives. The KPI tile 'Realized P&L (last 10 closed)' is the sum over exactly this slice, so the two always agree.

**Interactions.** None. Rows are inert. The only way this list changes is a successful Close (or a server-side expiry) followed by the 30s revalidate.

**Responsive.** The 12-column grid holds at every width — columns compress rather than wrap or scroll, so on narrow viewports the monospace strings tighten. No horizontal scroller is provided.

**Key copy.** 'No closed positions yet' · 'Closed positions appear here with realised P&L + exit reason.' · row prefix 'entry ' · '—' for missing exit reason or timestamp

<details><summary><b>Stitch prompt for this screen</b></summary>

```text
Design a dark, headerless audit list of closed options positions for an Indian NSE paper-trading app on a #0D0D0E canvas. Ten stacked rows, 8px apart, each a 12px-radius strip with a 1px #29292D border and a very subtle #151517 fill at about 40% opacity, 12px vertical and 12px horizontal padding. Every row is a 12-column grid with no column headers at all — alignment alone implies the structure. Left third: Geist Mono 12px #F7F7F8 reading 'NIFTY · Iron Condor', 'BANKNIFTY · Short Strangle', 'FINNIFTY · Bull Call Spread'. Next: Geist Mono 12px #96969E exit reasons 'target_hit', 'manual', 'expired', '—'. Next quarter: Geist Mono 12px #96969E timestamps '02 Aug, 03:20 pm', '31 Jul, 11:04 am'. Then a right-aligned Geist Mono tabular P&L column mixing #10B981 gains like +₹8,240 and +₹2,180 with #F5808C losses like −₹6,150 and −₹12,470, using a true minus sign, not a hyphen. Far right, a quieter right-aligned Geist Mono 10px #96969E column reading 'entry +₹8,240', 'entry −₹4,300'. No hover states, no buttons, no chevrons — this is a static ledger. Also show the empty state: a recessed 12px-radius #1E1E21 well with a 44px circular outlined check icon, a 16px semibold white heading 'No closed positions yet' and a 14px #D3D3D7 line 'Closed positions appear here with realised P&L + exit reason.'
```
</details>

---

## `/fno?tab=lab#chain` — Live option chain (OI/LTP) + click-to-build strategy builder

**File** `/Users/rishi/QuantX/frontend/components/fno/FoStrategiesWorkspace.tsx (OptionChainPanel L1316-1522; ChainTradingTable L1527-1626; BuilderPanel L1632-1813; SelectedLeg type L1309-1314)` · **490 LOC** · **Access** Authenticated AND broker-linked. Without a broker the entire panel (toolbar included) is replaced by a connect-broker EmptyState.

**Shell** — Inside the workspace shell, TabsContent value='chain' className='mt-4' → a space-y-12 column: toolbar, then the active view, then the conditional Builder, then a caption.

**Purpose.** The conventional NSE strike-centric chain — CE columns left, strike spine in the middle, PE columns right — where the LTP cells are the leg picker. Clicking builds up to four legs, which materialise a sticky bottom Builder that auto-names the structure and deploys it as a custom paper position.

### Anatomy (render order)

| # | Region | What renders |
|---|---|---|
| 1 | **Toolbar** | flex flex-wrap items-center gap-8. (a) SYMBOL segmented pill: inline-flex gap-2, radius 9999, 1px #29292D, p-2; three buttons radius 9999, px-10 py-4, Geist Mono 10px uppercase tracking-wider — NIFTY / BANKNIFTY / FINNIFTY; active = .glass-control-accent (#406AE4 fill, white ink, elev-1), inactive = #96969E hovering to #F7F7F8. (b) VIEW segmented pill, same construction, five buttons whose LABELS DIFFER FROM THEIR VALUES: value 'trading' renders 'OI / LTP', then 'Greeks', 'Smile', 'Term', 'Cone'. (c) ghost Button sm with a bare RefreshCw 14px that spins while loading. (d) ml-auto status group, flex gap-12, Geist Mono 10px #96969E: 'Spot ₹24,180.5' (numMax at 2 decimals) and 'Expiry 2026-08-06' (taken from rows[0].expiry). |
| 2 | **Chain table frame** | div overflow-x-auto, radius 6 (rounded-xs — the table-cell radius step), 1px #29292D. Header and body both carry min-w-[560px], so below 560px the table scrolls horizontally inside its own frame. |
| 3 | **Chain header row** | grid grid-cols-9 gap-8, border-b 1px #29292D, bg #151517/80, px-8 py-6, Geist Mono 9px uppercase tracking-wider #96969E. Nine single-span columns in exact order and alignment: 'CE OI' right · 'CE Vol' right · 'CE LTP' right · 'CE Bid' right · 'Strike' CENTER with bg #406AE4/05 and ink #8FB0FF (the spine is tinted) · 'PE Ask' left · 'PE LTP' left · 'PE Vol' left · 'PE OI' left. Note the deliberate asymmetry: the CE side shows Bid, the PE side shows Ask. |
| 4 | **Chain body rows** | div max-h-[60vh] overflow-y-auto (so the header stays put while strikes scroll). Each row: grid grid-cols-9 items-center gap-8, border-b 1px #29292D/60 with last:border-b-0, px-8 py-4, Geist Mono 11px, hover bg #1E1E21/40. OI and Volume are compacted client-side as `(v/1000).toFixed(0) + 'K'` → '1,240,000' renders '1240K' (no grouping, no decimal). Bid/Ask are toFixed(2). The strike is centered, weight 600, #F7F7F8, tabular-nums. Missing values render '—'. |
| 5 | **LTP cells = the leg picker** | CE LTP and PE LTP are real <button> elements, not spans. Both: col-span-1, -my-2, radius 4 (`rounded`), px-4 py-2, weight 600, tabular-nums, 90ms colour transition. CE is text-RIGHT and tinted #10B981; PE is text-LEFT and tinted #F5808C. title attribute on both: 'Click to BUY · Shift+click to SELL'. Disabled (and visually inert — disabled:bg-transparent, disabled:hover:bg-transparent, default cursor) when there is no LTP. Unselected hover: bg #406AE4/10. SELECTED-BUY: bg #10B981/20, ink #10B981, 1px ring #10B981/50. SELECTED-SELL: bg #F5808C/20, ink #F5808C, 1px ring #F5808C/50. |
| 6 | **Builder panel (appears at ≥1 leg)** | Card with className 'sticky bottom-2 border-primary/40 bg-wrap/95 shadow-lg backdrop-blur' — sticky at 8px from the viewport bottom, 1px #406AE4/40 accent border, 95%-opaque #151517 with a backdrop blur (this is one of the few legitimately floating surfaces). CardHeader: Sparkles 16px #8FB0FF + 'Builder · ' in weight 600 #F7F7F8 with the detected strategy name in #8FB0FF; right, ghost Button sm 'Clear'. |
| 7 | **Builder body** | CardBody space-y-12. (1) A 12px #D3D3D7 description of the detected structure. (2) Leg rows in space-y-4, sorted by strike ascending with CE before PE at equal strikes: each is flex justify-between, radius 8, 1px #29292D, bg #151517/60, px-8 py-4, Geist Mono 12px — left group gap-8 shows the side in weight 600 (#10B981 BUY / #F5808C SELL), then '{CE\|PE} {strike}' in #F7F7F8, then '@ ₹142.65' in #96969E; right is a bare XCircle 14px button, #96969E hovering to #F5808C, aria-label='Remove leg'. (3) Economics + submit row: flex flex-wrap items-end justify-between gap-12 above a border-t 1px #29292D with pt-12 — left group holds 'NET PER SHARE' (Geist Mono 9px uppercase #96969E) over a Geist Mono 14px weight 600 tabular value that reads either 'Debit ₹78.30' tinted #F5808C or 'Credit ₹64.15' tinted #10B981 (sign convention: Σ BUY ltp − Σ SELL ltp; ≥0 is a debit and is shown in the DOWN colour), and a 'LOTS' NumericInput constrained to w-80px, min 1 max 20; right is a primary Button with a Radio glyph reading `Deploy 4-leg to paper` (leg count interpolated), swapping to a spinning Loader2 while deploying. |
| 8 | **Builder helper + panel caption** | Builder helper p at 10px #96969E: 'Click any CE/PE LTP cell to add a leg (click = BUY, Shift+click = SELL, click again to remove). Strikes resolve to PCT_OFFSET anchors so the backend executor can re-resolve at order placement.' Panel-level caption below everything, 10px #96969E: 'Streaming live from your connected broker · refreshes every 30s · 15s in-process cache so paper MTM uses the same prices.' |
| 9 | **NO payoff diagram (as built)** | The Builder's only economics preview is the single Debit/Credit line above. There is no payoff curve, no breakeven markers and no max-profit/max-loss projection in this panel — the payoff explorer is the sibling outer tab /fno?tab=payoff (components/fno/PayoffCalculator.tsx) and is never mounted here. |

**Components** — `OptionChainPanel` · `ChainTradingTable` · `BuilderPanel` · `Card (sticky, floating variant)` · `CardHeader` · `CardBody` · `NumericInput (w-80px)` · `Button ghost sm / primary` · `Skeleton` · `EmptyState` · `toast.error / toast.success` · `lucide: RefreshCw, Sparkles, XCircle, Radio, Loader2, Target`

**States & data.** GET /api/fo-strategies/chain/{symbol} (SWR key ['fo:chain', symbol], 30s refresh, 15s dedupe). Rows carry {strike, option_type, expiry, ltp, bid, ask, oi, volume, iv, delta, gamma, theta, vega, tradingsymbol}; a useMemo pairs CE+PE into one object per strike and sorts ascending (the `?? []` lives INSIDE the memo deliberately — a code comment records that hoisting it re-grouped the widest table in the app on every render). BROKER-UNAVAILABLE (source === 'unavailable'): the whole panel short-circuits to an EmptyState with a 32px Target glyph, title 'Connect a broker for the live option chain', description 'The chain streams real-time from your Zerodha / Upstox / Angel account. Without a connected broker we fall back to model-estimated premiums for paper MTM.' LOADING: Skeleton w='100%' h='300px'. NO ROWS: EmptyState 'No chain rows returned' / 'Try a different symbol, or check the broker connection.' Selection state: SelectedLeg = {side, option_type, strike, ltp}, capped at 4.

**Interactions.** Plain click on an LTP cell adds a BUY leg; Shift+click adds a SELL leg. Clicking the same strike+type with the SAME side removes it; clicking with the OPPOSITE side flips it in place. A fifth leg is refused with toast.error 'Max 4 legs' / 'Iron Condor is the most complex template supported.' STRATEGY AUTO-NAMING covers: 1 leg → 'Long Call' / 'Short Call' / 'Long Put' / 'Short Put'; 2 CE legs → 'Bull Call Spread' (buy low, sell high) or 'Bear Call Spread' (sell low, buy high); 2 PE legs → 'Bear Put Spread' (sell low, buy high) or 'Bull Put Spread' (buy low, sell high); 1 CE + 1 PE same side → 'Long/Short Straddle' at equal strikes, 'Long/Short Strangle' otherwise; 4 legs with 2 buys / 2 sells / 2 CE / 2 PE → 'Iron Butterfly' when both short strikes match, else 'Iron Condor'; anything else → 'Custom (N legs)'. Deploy POSTs /api/fo-strategies/paper/open with {symbol, lots, legs:[{side lowercased, option_type, strike_anchor:'PCT_OFFSET', strike_offset: +(((strike/spot)-1)*100).toFixed(4), expiry:'CURRENT_WEEK', qty_lots:1}]} — strikes are converted to percentage offsets from spot so the executor can re-resolve them. Success → toast.success 'Custom position deployed' with `Net premium +₹8,240 · 2 lot(s)`, then the selection clears and lots reset to 1. Failure → toast.error 'Deploy failed'. 'Clear' wipes the selection and resets lots.

**Responsive.** The table frame scrolls horizontally below 560px (min-w-[560px] + overflow-x-auto) and vertically at 60vh. The toolbar wraps freely; the spot/expiry status group keeps ml-auto until it wraps to its own line. The Builder is sticky at bottom-2 (8px) and spans the content width; its economics row wraps (flex-wrap items-end) so the Deploy button drops below the Net/Lots group on narrow viewports.

**Key copy.** 'OI / LTP' · 'Greeks' · 'Smile' · 'Term' · 'Cone' · 'Spot ₹24,180.5' · 'Expiry 2026-08-06' · 'Click to BUY · Shift+click to SELL' · 'Connect a broker for the live option chain' · 'The chain streams real-time from your Zerodha / Upstox / Angel account. Without a connected broker we fall back to model-estimated premiums for paper MTM.' · 'No chain rows returned' · 'Try a different symbol, or check the broker connection.' · 'Builder · Iron Condor' · 'Clear' · 'NET PER SHARE' · 'Debit ₹78.30' / 'Credit ₹64.15' · 'Deploy 4-leg to paper' · 'Max 4 legs' / 'Iron Condor is the most complex template supported.' · 'Click any CE/PE LTP cell to add a leg (click = BUY, Shift+click = SELL, click again to remove). Strikes resolve to PCT_OFFSET anchors so the backend executor can re-resolve at order placement.' · 'Streaming live from your connected broker · refreshes every 30s · 15s in-process cache so paper MTM uses the same prices.'

<details><summary><b>Stitch prompt for this screen</b></summary>

```text
Design a dark NSE option-chain screen with a click-to-build strategy panel, on a #0D0D0E canvas. Toolbar: two fully-rounded segmented pill groups with 1px #29292D borders — NIFTY / BANKNIFTY / FINNIFTY with NIFTY filled solid #406AE4 white text, and OI / LTP · Greeks · Smile · Term · Cone with 'OI / LTP' filled; all labels Geist Mono 10px uppercase letter-spaced. A small refresh icon button, then right-aligned Geist Mono 10px #96969E 'Spot ₹24,180.5   Expiry 2026-08-06'. Below, a 6px-radius bordered table with a 9-column grid. Header on a #151517 fill in Geist Mono 9px uppercase #96969E: right-aligned CE OI, CE Vol, CE LTP, CE Bid; a CENTER 'Strike' column tinted with a 5% #406AE4 wash and #8FB0FF text; then left-aligned PE Ask, PE LTP, PE Vol, PE OI. Twelve monospace 11px rows on hairline dividers, strikes 23800 to 24500 stepping 100, centered and bold white. Example row: '842K  318K  142.65  141.90 | 24200 | 96.40  97.15  402K  1180K'. CE LTP values are #10B981 and PE LTP values are #F5808C, rendered as small tappable chips; show two chips selected — one with a green 20% fill and green ring, one with a rose 20% fill and rose ring. At the bottom, a sticky floating card with a 1px #406AE4 40% border and a blurred 95% #151517 fill titled 'Builder · Iron Condor' with a sparkle glyph and a Clear button; inside, four monospace leg rows like 'SELL CE 24300 @ ₹142.65' each with an x, plus 'NET PER SHARE  Credit ₹64.15' in #10B981, a small Lots stepper showing 2, and a solid #406AE4 button 'Deploy 4-leg to paper'.
```
</details>

---

## `/fno?tab=lab#chain-analytics` — Chain analytics views — Greeks table, Volatility Smile, Term Structure, Vol Cone

**File** `/Users/rishi/QuantX/frontend/components/fno/FoStrategiesWorkspace.tsx (ChainGreeksTable L2341-2407; ChainSmileChart L1821-2005; TermStructureChart L2011-2160; VolConeChart L2170-2337; Kpi L1030-1041)` · **587 LOC** · **Access** Authenticated + broker-linked. Term structure has its own broker-unavailable EmptyState; the cone degrades to realised-vol only.

**Shell** — Same OptionChainPanel toolbar and space-y-12 column as the trading view; only the body between the toolbar and the caption swaps. The Builder can still be open beneath (selected legs persist across view switches).

**Purpose.** The four non-trading views behind the Chain tab's view switcher. Each replaces the OI/LTP table entirely: a Greeks grid at the same strike-centric geometry, and three Recharts panels (smile, term structure, vol cone) each fronted by its own four-tile KPI strip and closed by an explanatory caption.

### Anatomy (render order)

| # | Region | What renders |
|---|---|---|
| 1 | **Greeks table** | Same frame as the trading table — overflow-x-auto, radius 6, 1px #29292D — but an ELEVEN-column grid with min-w-[640px]. Header: grid-cols-11 gap-8, border-b 1px #29292D, bg #151517/80, px-8 py-6, Geist Mono 9px uppercase tracking-wider #96969E, using the actual Greek letters: 'CE IV' · 'CE Δ' · 'CE Γ' · 'CE Θ' · 'CE V' (all right-aligned) · 'Strike' (center, bg #406AE4/05, ink #8FB0FF) · 'PE V' · 'PE Θ' · 'PE Γ' · 'PE Δ' · 'PE IV' (all left-aligned) — a mirrored layout that puts IV at both outer edges. Body max-h-[60vh] overflow-y-auto; rows grid-cols-11 items-center gap-8, border-b 1px #29292D/60, px-8 py-4, Geist Mono 11px, hover bg #1E1E21 (full strength here, vs /40 on the trading table). PRECISION AND TINT, exactly as built: IV = (iv*100).toFixed(1) + '%' in #D3D3D7; Δ 3 decimals — CE Δ in #10B981, PE Δ in #F5808C; Γ 5 decimals in #96969E; Θ 2 decimals in #F5808C on BOTH sides; Vega 2 decimals in #96969E. Nulls render '—'. Cells are inert — no click-to-build on this view. |
| 2 | **Smile — KPI strip** | grid-cols-2 gap-12 md:grid-cols-4 of Kpi wells (radius 12, 1px #29292D, bg #151517/60, p-8; label Geist Mono 9px uppercase tracking-wider #96969E; value mt-2 Geist Mono 14px weight 600 tabular-nums). Four tiles: 'ATM STRIKE' (nearest strike to spot), 'ATM IV' as '13.4%', 'SKEW (PE WING − CE WING)' rendered '+1.85 pts' and tinted #F5808C when ≥0 / #10B981 when <0 (put skew is the NIFTY norm, so positive skew reads in the DOWN colour), 'STRIKES PLOTTED' as a count. |
| 3 | **Smile — chart** | Container radius 6, 1px #29292D, bg #151517/40, p-12. ResponsiveContainer width 100%, HEIGHT 360. ComposedChart margin {top:10, right:20, left:10, bottom:10}. CartesianGrid strokeDasharray '3 3' stroke rgba(28,30,41,0.4). XAxis dataKey='strike', type number, domain ['dataMin','dataMax'], ticks 10px monospace var(--color-muted), axis stroke var(--color-line). YAxis tickFormatter `${v.toFixed(1)}%`, domain ['auto','auto'], rotated -90° label 'Implied vol' at insideLeft, 10px monospace muted. Tooltip: bg color-mix(var(--color-wrap-hover) 95%), 1px var(--color-line), radius 8, 11px monospace; label style ink var(--color-desc) weight 600; series relabelled 'CE IV' / 'PE IV'; label formatter 'Strike 24200'. Legend 11px monospace, same relabelling. ReferenceLine at the ATM strike: stroke var(--chart-primary) (#8FB0FF dark / #406AE4 light), strokeDasharray '4 4', top label 'ATM'. Two monotone Lines, strokeWidth 2, dot r=2, activeDot r=4, connectNulls: ceIv in var(--color-up) #10B981, peIv in var(--color-down) #F5808C. |
| 4 | **Smile — empty + caption** | EMPTY: EmptyState Target, 'No IV data to plot' / 'Smile needs at least one strike with a non-zero LTP so we can imply σ. Check back after market open.' CAPTION p 10px #96969E: 'Implied vol per strike, derived from the live broker option chain LTPs. Skew = average PE IV in the 5 strikes below ATM minus average CE IV in the 5 strikes above. Positive skew (red > green on the left wing) is the NIFTY norm — put hedges are pricier than equivalent upside calls.' |
| 5 | **Term structure** | LOADING Skeleton w='100%' h='380px'. KPI strip of four: 'SPOT' as '₹24,180.5', 'FRONT EXPIRY IV' '14.2%', 'BACK EXPIRY IV' '12.8%', 'SHAPE' rendering 'Flat' (#F7F7F8) / 'Contango' (#10B981) / 'Backwardation' (#F5808C). Chart container identical, ResponsiveContainer HEIGHT 340. XAxis dataKey='label' where each label is built as `${days_to_expiry}d (${expiry.slice(5)})` → '3d (08-06)', '10d (08-13)', '24d (08-27)', '52d (09-24)'. YAxis label 'ATM IV'. Three Lines: ceIv var(--color-up) width 1.5 DASHED '3 3' dot r2 connectNulls; peIv var(--color-down) width 1.5 DASHED '3 3' dot r2 connectNulls; atmIv var(--chart-primary) width 2.5 SOLID dot r4 activeDot r6 — the accent line is the visual hero. Tooltip relabels to 'ATM mean' / 'CE IV' / 'PE IV'. |
| 6 | **Term — states + caption** | BROKER-UNAVAILABLE: EmptyState Target, 'Connect a broker to see term structure' / 'Term structure pulls chains for current week / next week / current month / next month. All four require a connected broker.' NO DATA: 'No IV data across expiries' / 'The broker returned no priceable strikes near ATM on any expiry. Check back after market open.' CAPTION is composed: the shape note first — flat 'Near + far vol roughly equal.', contango 'Back > front — normal. Longer vega exposure commands more premium.', backwardation 'Front > back — event premium baked into the near expiry (earnings, RBI/Fed, expiry effect).' — followed by 'Each dot is the ATM IV at one expiry; the blue solid line is the mean of CE + PE at ATM, dashed lines show each side separately. Front-month skews can hide an event premium — if front is much higher than back, the market is pricing something specific into the near contract.' |
| 7 | **Vol cone** | LOADING Skeleton h='380px'. KPI strip of four: 'SYMBOL' as 'NIFTY · spot ₹24,181', 'SAMPLE SIZE' as '248 bars', 'NEAREST IV' as '14.2%', 'RICHNESS VS HISTORY' with five tinted verdicts — 'RICH (>p90)' #F5808C, 'Elevated (p75-p90)' #F0A94F, 'CHEAP (<p10)' #10B981, 'Below normal (p10-p25)' #10B981, 'Normal (p25-p75)' #F7F7F8. Chart container identical, HEIGHT 360. XAxis dataKey='window' over '7d','14d','21d','30d','60d','90d' with an insideBottom label 'Realised vol window' at offset -5. YAxis tickFormatter `${v.toFixed(0)}%`, rotated label 'Annualised vol'. Series: Line p50 stroke var(--color-muted) width 2 dot r3, legend name 'p50 (median)'; Line current_rv stroke var(--color-warning) #F0A94F width 2 dot r4, name 'Current RV'; Line p10 and Line p90 both stroke the LITERAL HEX '#444' dashed '2 2' with no dots (the only hard-coded colour in the file); Scatter 'Current IV' fill var(--chart-primary) shape 'diamond' overlaying the ATM IVs onto their nearest window. |
| 8 | **Cone — empty + caption** | EMPTY: EmptyState Target, 'No vol cone data' / 'Need ~100 days of bars to build the percentile distribution. Check back later.' CAPTION: 'Cone shape = where realised vol has spent its time over the past year. Above p90 = rich (options pricing in event premium); below p10 = cheap (mean-reversion candidate for net-vol buyers). Blue diamonds are current ATM implied vols per expiry; orange dots are the latest realised vol per window.' |

**Components** — `ChainGreeksTable` · `ChainSmileChart` · `TermStructureChart` · `VolConeChart` · `Kpi ×4 per chart view` · `Recharts: ComposedChart, ResponsiveContainer, CartesianGrid, XAxis, YAxis, Tooltip, Legend, ReferenceLine, Line, Scatter` · `Skeleton` · `EmptyState`

**States & data.** Greeks and Smile reuse the chain SWR payload (30s). Term structure: GET /api/fo-strategies/term-structure/{symbol} (SWR ['fo:term', symbol], 60s refresh, 30s dedupe) returning {symbol, spot, source, shape, expiries:[{anchor, expiry, days_to_expiry, atm_strike, atm_iv, ce_iv, pe_iv}]}. Vol cone: GET /api/fo-strategies/vol-cone/{symbol} (SWR ['fo:cone', symbol], 5min refresh, 60s dedupe) returning {symbol, spot, source, windows:[{window_days, p10, p25, p50, p75, p90, current_rv, samples}], current_ivs:[{expiry, days, window_days, iv}]}. All IVs arrive as ratios and are multiplied by 100 client-side then fixed to 2 decimals. Skew is computed in the component: mean PE IV over the 5 strikes at-and-below ATM minus mean CE IV over the 5 strikes at-and-above, to 2 decimals. AS-BUILT NOTE: the cone's percentile bands are rendered as four individual lines (p10, p50, p90 plus current RV), NOT as the stacked shaded areas the code comment describes — the p25/p75 deltas are computed into the row objects but never bound to a series.

**Interactions.** The view switcher in the toolbar is the only control; switching keeps the symbol and any selected builder legs. The Greeks table is read-only. Charts expose Recharts hover tooltips and legend labels only — no brushing, zooming, click-through or crosshair sync. Term and cone each own their SWR key, so switching to them triggers a fresh fetch on first view and then their own polling cadence.

**Responsive.** KPI strips are 2-up below md (768px) and 4-up above. The Greeks table scrolls horizontally below 640px (min-w-[640px]) and vertically at 60vh. Charts use ResponsiveContainer width 100% with FIXED pixel heights — 360 (smile), 340 (term), 360 (cone) — so they never shrink vertically on mobile; axis tick labels at 10px may crowd on narrow screens.

**Key copy.** 'CE IV / CE Δ / CE Γ / CE Θ / CE V | Strike | PE V / PE Θ / PE Γ / PE Δ / PE IV' · 'ATM STRIKE' · 'ATM IV' · 'SKEW (PE WING − CE WING)' · 'STRIKES PLOTTED' · 'No IV data to plot' · 'Smile needs at least one strike with a non-zero LTP so we can imply σ. Check back after market open.' · 'Connect a broker to see term structure' · 'Contango' / 'Backwardation' / 'Flat' · 'Front > back — event premium baked into the near expiry (earnings, RBI/Fed, expiry effect).' · 'RICH (>p90)' / 'Elevated (p75-p90)' / 'CHEAP (<p10)' / 'Below normal (p10-p25)' / 'Normal (p25-p75)' · 'Realised vol window' · 'Annualised vol' · 'p50 (median)' · 'Current RV' · 'Current IV'

<details><summary><b>Stitch prompt for this screen</b></summary>

```text
Design two dark analytics views for an Indian NSE options desk on a #0D0D0E canvas. VIEW A — a Greeks grid: a 6px-radius bordered table with an 11-column mirrored layout. Header in Geist Mono 9px uppercase #96969E: right-aligned CE IV, CE Δ, CE Γ, CE Θ, CE V; a centered 'Strike' column washed with 5% #406AE4 and lettered #8FB0FF; then left-aligned PE V, PE Θ, PE Γ, PE Δ, PE IV. Ten monospace 11px rows, strikes 23800–24700 in 100 steps, e.g. '13.9%  0.612  0.00042  −18.40  12.85 | 24200 | 13.10  −16.90  0.00044  −0.388  14.4%'. Call deltas in #10B981, put deltas in #F5808C, both thetas in #F5808C, gamma and vega in #96969E, IV in #D3D3D7, all tabular. VIEW B — a volatility smile panel: a four-tile KPI row of 12px-radius wells with Geist Mono 9px uppercase #96969E labels over 14px semibold tabular values — 'ATM STRIKE' 24200, 'ATM IV' 13.4%, 'SKEW (PE WING − CE WING)' +1.85 pts in #F5808C, 'STRIKES PLOTTED' 34. Beneath, a 360px-tall line chart in a 6px-radius bordered container with a faint dashed grid: x-axis strikes 23600 to 24800 in monospace 10px #96969E, y-axis implied vol 11%–18% with a rotated 'Implied vol' label. Two 2px monotone curves with small round dots — a green #10B981 CE IV line and a rose #F5808C PE IV line that rises steeply on the left wing — plus a vertical dashed #8FB0FF reference line labelled 'ATM' at 24200. Monospace legend, dark tooltip with an 8px radius.
```
</details>

---

## `/fno?tab=lab#ai-suggest` — Ask the AI copilot — plain-English view to a pre-priced structure

**File** `/Users/rishi/QuantX/frontend/components/fno/FoStrategiesWorkspace.tsx (Dialog L591-932; handleAskAI L181-200; deployAISuggestion L202-224)` · **385 LOC** · **Access** Authenticated. The 'Include my positions' path additionally reads the user's live equity + option book server-side.

**Shell** — Radix Dialog, overlay z-40 rgba(0,0,0,.60), content z-50 centered, w-full !max-w-xl → 576px, radius 8, 1px #29292D, bg #151517, p-20, elev-1. Body is one space-y-12 column.

**Purpose.** The workspace's deepest modal: the user types a market view, optionally opts into portfolio-aware hedge sizing (and a focus symbol), and the copilot returns one template with lot sizing, reasoning, confidence, live regime/VIX context, an optional net-delta book summary with clickable per-symbol exposures, and pre-priced legs — which the user must then explicitly deploy.

### Anatomy (render order)

| # | Region | What renders |
|---|---|---|
| 1 | **Title + intro** | Dialog title 'Ask the AI copilot for an options structure' at 16px weight 400. Intro p at 12px #D3D3D7: 'Describe your market view in plain English. The copilot reads the live regime + VIX and picks one multi-leg structure that fits. You stay in control: you click Deploy, the AI never places a trade for you.' |
| 2 | **Inputs row** | grid-cols-2 gap-8. LEFT: a Geist Mono 9px uppercase tracking-wider #96969E label 'Underlying' over a full-width segmented pill (mt-4, inline-flex w-full, gap-2, radius 9999, 1px #29292D, p-2) whose three buttons are flex-1, radius 9999, px-8 py-4, Geist Mono 10px uppercase tracking-wider — NIFTY / BANKNIFTY / FINNIFTY, active = .glass-control-accent. RIGHT: label 'Capital (optional, ₹)' in the same micro style over a NumericInput id='ai-capital' with placeholder 'e.g. 200000', min 10,000, max 10,000,000. |
| 3 | **Prompt textarea** | Label 'Your view'. The wrapper is the only AI-tinted control in the file: mt-4, radius 16 (rounded-lg), bg #151517, p-4, with an INLINE border colour of color-mix(in srgb, {AI} 28%, transparent) where AI = #8FB0FF dark / #3459C9 light, and focus-within:border-ai/60. Inside, a textarea rows=3, maxLength=600, w-full resize-none, radius 12, transparent bg, px-12 py-8, 14px #F7F7F8, placeholder in #96969E reading: e.g. 'I think Nifty will stay range-bound between 23800-24300 this week. Want defined risk, prefer collecting premium.' Below, a live counter p at 10px #96969E: '0 / 600 chars'. |
| 4 | **Portfolio toggle** | A whole-row <label>: flex items-start gap-8, radius 12, 1px #29292D, bg #151517/60, px-12 py-8, 12px #D3D3D7. A 14px native checkbox at mt-2, radius from the border-line token, bg #0D0D0E, accent-color #406AE4. Text stack: bold line 'Include my positions for hedge sizing' in weight 500 #F7F7F8, then a <br>, then a 10px #96969E explainer: 'Loads your open equity + option positions, computes net delta exposure, and lets the AI size a hedge that offsets it. Best for “hedge my book” prompts.' (curly quotes as built). |
| 5 | **Focus symbol (conditional)** | Rendered ONLY while the portfolio toggle is on. Label 'Focus symbol (optional)'. A text input mt-4, w-full, radius 12, 1px #29292D, bg #0D0D0E, px-12 py-6, Geist Mono 12px, text-transform uppercase, focus:border #406AE4, maxLength 20, placeholder 'e.g. RELIANCE — leave empty to hedge whole book' with placeholder:normal-case so the hint is not uppercased. Helper p 10px #96969E: 'Narrows the hedge to a single underlying's exposure instead of the net book.' |
| 6 | **Quick-fire chips** | Shown only before a result exists. flex flex-wrap gap-4; each is a .glass-control pill (bg #1E1E21, 1px #29292D), radius 9999, px-10 py-4, 10px #D3D3D7 hovering to #F7F7F8. Exactly five, in order: 'Bullish on Nifty next week, want defined risk' · 'Bearish on Bank Nifty, expecting a sharp drop' · 'Expect a big move on RBI day — direction unclear' · 'Range-bound view, want to collect premium safely' · 'Hedge my book against a 3% downside move'. The last one ALSO switches the portfolio toggle on when clicked. |
| 7 | **Result — recommendation block** | Result region is space-y-12 above a border-t 1px #29292D with pt-12. First: a card radius 12, 1px #406AE4/30, bg #406AE4/[0.04], p-12, flex items-start gap-8 — a 16px Brain glyph in #8FB0FF at mt-2, then a text column: 'The AI copilot recommends:' at 12px #D3D3D7, then a weight-600 #F7F7F8 line '{Strategy Label} · ' with the lot count in Geist Mono 14px #8FB0FF ('2 lots', singularising to 'lot' at 1), then the model's reasoning at mt-8, 12px, leading-relaxed, #D3D3D7. |
| 8 | **Result — meta grid** | grid-cols-2 gap-8 at 11px of four SmallMeta wells: 'EXPECTED OUTCOME', 'RISK', 'REGIME / VIX' formatted `${regime} · ${vix.toFixed(2)} (${vix_direction})`, and 'CONFIDENCE' as a rounded whole percent defaulting to 50% when absent. |
| 9 | **Result — portfolio context tile (conditional)** | Only when portfolio_context exists AND has_positions. Container space-y-8, radius 12, 1px #406AE4/20, bg #406AE4/[0.04], p-8. Heading in Geist Mono 9px uppercase tracking-wider #8FB0FF: 'Your book context (factored into the suggestion)'. Body in Geist Mono 11px #F7F7F8 across three <br>-separated lines: 'Equity: 6 positions · delta +₹4,82,000', 'Options: 2 positions · delta −₹1,10,400', then a weight-600 line 'NET DELTA +₹3,71,600 (LONG bias)' tinted #10B981 when ≥0 / #F5808C when <0 — the parenthetical is 'near-flat' when \|net delta\| < 50,000, else 'LONG bias' / 'SHORT bias'. |
| 10 | **Result — per-symbol exposures** | Nested well: radius 8, 1px #29292D, bg #151517/40, p-8. Heading Geist Mono 9px uppercase tracking-wider #96969E: 'Top exposures · click to focus next ask'. Then up to FIVE rows sorted by descending \|total_delta_inr\|, each a full-width <button>: flex justify-between, radius 4, px-6 py-4, text-left, Geist Mono 11px, hover bg #1E1E21; when it matches the current focus symbol it gains bg #406AE4/10 plus a 1px #406AE4/30 ring. Left is the symbol in #F7F7F8 (RELIANCE, HDFCBANK, TCS, INFY…); right is a gap-8 group of the tabular signed delta tinted up/down, then a Badge at 9px whose tone maps LONG→up, SHORT→down, anything else→primary, carrying the literal bias text. |
| 11 | **Result — pre-priced legs** | Well radius 12, 1px #29292D, bg #151517/60, p-8. Heading 'Pre-priced legs' in Geist Mono 9px uppercase tracking-wider #96969E. ul space-y-4 in Geist Mono 11px, rows identical to the recommendation card: colored BUY/SELL + type + strike on the left, '@ ₹142.65' in #96969E on the right. Footer row mt-8, flex justify-between, 10px #96969E: 'Net premium +₹8,240' on the left; 'Max profit +₹8,240 · Max loss −₹16,760' (or '· Max loss Unbounded') on the right. |
| 12 | **Action bar** | flex items-center justify-end gap-8, border-t 1px #29292D, pt-12. BEFORE a result: ghost 'Close' + primary Button 'Suggest a structure' with a 14px Brain glyph at mr-4, DISABLED until the trimmed prompt is at least 4 characters, showing a spinning Loader2 while in flight. AFTER a result: ghost 'Close', secondary Button 'Ask again' (clears the result and error but keeps the prompt), and a hand-rolled 36px-tall radius-9999 .glass-control-accent pill at 13px/600 with a 14px Radio glyph reading 'Deploy AI suggestion'. |

**Components** — `Dialog (Radix, !max-w-xl)` · `segmented pill group` · `NumericInput` · `raw textarea with AI-tinted wrapper` · `native checkbox with accent-primary` · `raw uppercase text input` · `quick-fire .glass-control chips ×5` · `SmallMeta ×4` · `Badge (up/down/primary)` · `Button ghost / secondary / primary` · `raw .glass-control-accent pill` · `lucide: Brain, Loader2, Radio` · `toast`

**States & data.** POST /api/fo-strategies/ai-suggest with {prompt (trimmed), symbol, capital_inr (omitted when blank), include_portfolio, focus_symbol (trimmed + uppercased, omitted when blank)}. Response: {template, symbol, spot, lots_suggestion, reasoning, expected_outcome, risk_summary, confidence, context:{regime, vix, vix_direction}, portfolio_context:{has_positions, equity_delta_inr, option_delta_inr, net_delta_inr, equity_count, options_count, by_symbol:{SYM:{equity_delta_inr, option_delta_inr, total_delta_inr, equity_qty, options_count, bias}}} | null, proposal: FoStrategyProposal | null}. Error path renders an inline block (radius 12, 1px #F5808C/30, bg #F5808C/05, p-12, 12px #F5808C) rather than a toast. `aiSuggesting` blocks dismissal — onClose returns early mid-request. NOTE: response.spot is not rendered.

**Interactions.** A quick-fire chip fills the textarea (and the hedge chip also flips the portfolio toggle). 'Suggest a structure' requires ≥4 characters. Clicking a per-symbol exposure row sets aiFocusSymbol, which both highlights that row and narrows the NEXT ask. 'Ask again' clears only the result so the same prompt can be re-run. 'Deploy AI suggestion' POSTs /api/fo-strategies/paper/open with {template, symbol, lots: lots_suggestion} — on success toast.success `Deployed AI suggestion · iron_condor` (the RAW template slug, not the label) with `Net premium +₹8,240 · 2 lot(s)`, then closes the dialog and resets result, prompt and capital; on failure toast.error 'Deploy failed'. The deploy path deliberately mirrors the manual one — the AI never places a trade itself, which the intro copy states.

**Responsive.** Fixed 576px cap, w-full below. The 2-column inputs row does not collapse. The exposures list, meta grid and chips all wrap. Long reasoning text simply extends the panel; there is no internal scroll cap, so on short viewports the modal can exceed the fold.

**Key copy.** 'Ask the AI copilot for an options structure' · 'Describe your market view in plain English. The copilot reads the live regime + VIX and picks one multi-leg structure that fits. You stay in control: you click Deploy, the AI never places a trade for you.' · 'Underlying' / 'Capital (optional, ₹)' / 'Your view' · 'I think Nifty will stay range-bound between 23800-24300 this week. Want defined risk, prefer collecting premium.' · '0 / 600 chars' · 'Include my positions for hedge sizing' · 'Loads your open equity + option positions, computes net delta exposure, and lets the AI size a hedge that offsets it. Best for “hedge my book” prompts.' · 'Focus symbol (optional)' · 'e.g. RELIANCE — leave empty to hedge whole book' · 'Narrows the hedge to a single underlying's exposure instead of the net book.' · the five quick-fire prompts · 'The AI copilot recommends:' · 'Your book context (factored into the suggestion)' · 'NET DELTA … (LONG bias / SHORT bias / near-flat)' · 'Top exposures · click to focus next ask' · 'Pre-priced legs' · 'Ask again' · 'Suggest a structure' · 'Deploy AI suggestion'

<details><summary><b>Stitch prompt for this screen</b></summary>

```text
Design a 576px-wide dark AI assistant modal for an Indian options desk, centered on a 60%-black scrim. Panel: 8px radius, #151517, 1px #29292D border, 20px padding. Title 16px regular white 'Ask the AI copilot for an options structure'; intro 12px #D3D3D7 'Describe your market view in plain English… you click Deploy, the AI never places a trade for you.' Two columns: a full-width segmented pill NIFTY / BANKNIFTY / FINNIFTY with NIFTY filled #406AE4, and a numeric field labelled 'CAPITAL (OPTIONAL, ₹)' placeholder 'e.g. 200000'. Below, a 16px-radius textarea container whose 1px border is a 28%-opacity #8FB0FF, holding three lines of 14px text: 'I think Nifty will stay range-bound between 23800-24300 this week. Want defined risk, prefer collecting premium.' with a 10px #96969E counter '112 / 600 chars'. Then a bordered checkbox row, checkbox accent #406AE4, bold white 'Include my positions for hedge sizing' over a 10px #96969E explainer. Five small rounded #1E1E21 chips of suggested prompts. Result block above a hairline: a card with a 30%-opacity #406AE4 border and a 4% blue wash holding a brain glyph, '​The AI copilot recommends:', a bold white 'Iron Condor · ' with '2 lots' in #8FB0FF, and reasoning text. A 2×2 grid of small wells: 'EXPECTED OUTCOME', 'RISK', 'REGIME / VIX  sideways · 13.42 (falling)', 'CONFIDENCE  72%'. A blue-tinted book tile in Geist Mono 11px: 'Equity: 6 positions · delta +₹4,82,000', 'Options: 2 · delta −₹1,10,400', 'NET DELTA +₹3,71,600 (LONG bias)' in #10B981, plus clickable rows RELIANCE +₹1,84,000 LONG, HDFCBANK +₹96,400 LONG, TCS −₹42,100 SHORT, INFY +₹18,900 LONG each with a tiny tone badge. Footer: ghost 'Close', secondary 'Ask again', solid rounded #406AE4 pill 'Deploy AI suggestion'.
```
</details>

---

## `/fno?tab=lab#backtest` — Inline template backtest — 180-day results modal

**File** `/Users/rishi/QuantX/frontend/components/fno/FoStrategiesWorkspace.tsx (Dialog L535-589; BacktestResultPanel L940-1028; Kpi L1030-1041; handleBacktest L226-244)` · **157 LOC** · **Access** Authenticated. Opened only from a RecommendationCard's Backtest button.

**Shell** — Radix Dialog, overlay z-40 rgba(0,0,0,.60), content z-50 centered, w-full !max-w-2xl → 672px, radius 8, 1px #29292D, bg #151517, p-20, elev-1. Body is a space-y-16 column.

**Purpose.** Runs a 180-day weekly-roll backtest of one recommended template on ₹1 lakh of starting capital and renders the summary as seven KPI tiles, a win-rate meter and a collapsible list of the last ten trade outcomes — without ever leaving the recommendations grid.

### Anatomy (render order)

| # | Region | What renders |
|---|---|---|
| 1 | **Title + method note** | Title at 16px weight 400: `Backtest — Iron Condor · NIFTY` (EM DASH between the word and the label, MIDDLE DOT before the symbol). Under it a 12px #D3D3D7 method note that interpolates the symbol: '180 days of historical NIFTY closes, weekly-roll convention, ₹1 Lakh starting capital. BS-mid premiums, σ from rolling realised vol.' |
| 2 | **Loading** | flex flex-col items-center gap-8 py-32: a 24px Loader2 spinning in #8FB0FF over a 12px #96969E line 'Running backtest — this can take 10-20 seconds…' (real ellipsis character). This is the state the modal OPENS in, because handleBacktest fires the request in the same tick it sets backtestFor. |
| 3 | **Error** | A block radius 12, 1px #F5808C/30, bg #F5808C/05, p-12, 12px ink #F5808C carrying the handleApiError string. No retry control. |
| 4 | **Headline KPI row** | grid-cols-2 gap-8 md:grid-cols-4 of Kpi wells (radius 12, 1px #29292D, bg #151517/60, p-8; label Geist Mono 9px uppercase tracking-wider #96969E; value mt-2 Geist Mono 14px weight 600 tabular-nums #F7F7F8). (1) 'TOTAL RETURN' as '+18.42%' with an explicit '+' for non-negatives, tinted #10B981 / #F5808C. (2) 'NET P&L' as inrSigned(final_capital − initial_capital), same tint rule. (3) 'SHARPE' to 2 decimals, neutral ink. (4) 'MAX DRAWDOWN' always tinted #F5808C and always prefixed with a literal '-' over the absolute value at 1 decimal → '-7.4%'. |
| 5 | **Trade-count row** | grid-cols-3 gap-8 of three more Kpi wells: 'TOTAL TRADES' neutral, 'WINNERS' tinted #10B981, 'LOSERS' tinted #F5808C. Losers falls back to totalTrades − winners when the field is absent. |
| 6 | **Win-rate meter** | A well radius 12, 1px #29292D, bg #151517/40, p-12. Label 'WIN RATE' in Geist Mono 9px uppercase tracking-wider #96969E. Below (mt-4, flex items-center gap-8): a track h-8px, flex-1, radius 9999, bg #29292D/60; the fill is h-full, radius 9999, SOLID #10B981 regardless of value, width clamped to `Math.min(100, Math.max(0, winRate*100))%`; to its right the numeric read-out in Geist Mono 12px weight 500 tabular-nums #F7F7F8 as '64.3%'. Note the win rate arrives as a 0-1 ratio and is scaled here. |
| 7 | **Trade log disclosure** | A native <details> element: radius 12, 1px #29292D, bg #151517/40, p-8. Summary is cursor-pointer 12px #D3D3D7 reading `Last 10 trade outcomes` (the number is min(10, trades.length)). Expanded content: ul mt-8 space-y-4 in Geist Mono 11px, one li per trade — flex justify-between with the date pair '2026-06-12 → 2026-06-19' in #96969E on the left (falling back to opened_at/closed_at) and the signed P&L on the right tinted #10B981 / #F5808C. Renders only when result.trades is a non-empty array. |
| 8 | **Action bar** | flex justify-end, border-t 1px #29292D, pt-12: a single ghost Button 'Close', disabled while the backtest is running. There is no re-run, no parameter control and no export. |

**Components** — `Dialog (Radix, !max-w-2xl)` · `BacktestResultPanel` · `Kpi ×7` · `native <details>/<summary>` · `Button ghost` · `lucide: Loader2, BarChart3`

**States & data.** POST /api/fo-strategies/backtest with {template: rec.strategy, symbol: rec.symbol, lookback_days: 180, initial_capital: 100000} — both numbers are hard-coded at the call site and are NOT user-adjustable. The response is untyped (Record<string, any>) and the panel defensively reads `result.summary ?? result` then accepts alias pairs: total_return_pct ?? return_pct, sharpe_ratio ?? sharpe, max_drawdown_pct ?? max_dd_pct, winners ?? winning_trades, losers ?? losing_trades ?? (total − winners), plus final_capital and initial_capital (defaulting to 100000). Net P&L is DERIVED as final_capital − initial_capital, so a response without final_capital shows −₹1,00,000. Trade rows accept pnl ?? net_pnl and entry_date ?? opened_at / exit_date ?? closed_at. backtestLoading blocks dismissal in onClose.

**Interactions.** Opening is instantaneous and always lands on the spinner. Closing clears backtestFor, backtestResult and backtestError together. The details disclosure is closed by default. No result is cached — reopening the same card re-runs the backtest.

**Responsive.** 672px cap, w-full below. Headline KPIs are 2-up below md (768px) and 4-up above; the trade-count row stays 3-up at every width. The trade log is a simple flex list that wraps rather than scrolling.

**Key copy.** 'Backtest — Iron Condor · NIFTY' · '180 days of historical NIFTY closes, weekly-roll convention, ₹1 Lakh starting capital. BS-mid premiums, σ from rolling realised vol.' · 'Running backtest — this can take 10-20 seconds…' · 'TOTAL RETURN' / 'NET P&L' / 'SHARPE' / 'MAX DRAWDOWN' / 'TOTAL TRADES' / 'WINNERS' / 'LOSERS' / 'WIN RATE' · 'Last 10 trade outcomes' · 'Close'

<details><summary><b>Stitch prompt for this screen</b></summary>

```text
Design a 672px-wide dark backtest results modal for an Indian NSE options strategy, centered on a 60%-black scrim. Panel: 8px radius, #151517 fill, 1px #29292D border, 20px padding. Title in 16px regular white: 'Backtest — Iron Condor · NIFTY'. Below it a 12px #D3D3D7 method note: '180 days of historical NIFTY closes, weekly-roll convention, ₹1 Lakh starting capital. BS-mid premiums, σ from rolling realised vol.' Then a row of four small wells (12px radius, 1px #29292D, faint #151517 fill, 8px padding), each with a Geist Mono 9px uppercase letter-spaced #96969E label above a Geist Mono 14px semibold tabular value: 'TOTAL RETURN' +18.42% in #10B981, 'NET P&L' +₹18,420 in #10B981, 'SHARPE' 1.34 in white, 'MAX DRAWDOWN' -7.4% in #F5808C. Under that a row of three more wells: 'TOTAL TRADES' 26, 'WINNERS' 17 in #10B981, 'LOSERS' 9 in #F5808C. Next a win-rate meter well: the label 'WIN RATE', an 8px-tall fully-rounded track in #29292D at 60% opacity filled 65% with solid #10B981, and '65.4%' at the right in Geist Mono 12px medium tabular white. Finally a collapsed disclosure row with a small triangle and 12px #D3D3D7 text 'Last 10 trade outcomes', shown expanded beneath it as monospace 11px rows: '2026-06-12 → 2026-06-19   +₹2,140' and '2026-06-19 → 2026-06-26   −₹1,860', dates in #96969E, gains #10B981, losses #F5808C with a true minus sign. Close with a hairline and a single right-aligned transparent 'Close' button. Also show the loading state: a centered spinner in #8FB0FF above 12px #96969E text 'Running backtest — this can take 10-20 seconds…'.
```
</details>

---
