# F&O hub — all six tabs

> Part of the Quant X as-built screen inventory — see [`../03-SCREEN-INVENTORY.md`](../03-SCREEN-INVENTORY.md)
> for the full index and [`../01-DESIGN-SYSTEM.md`](../01-DESIGN-SYSTEM.md) for every token used below.

**6 surfaces.**

## Family notes

SCOPE CORRECTION vs the brief: the brief mapped `components/scanner/FnoTab.tsx` to the `stocks` tab. As-built (app/(platform)/fno/page.tsx lines 132-142) `FnoTab` renders the **overview** tab; `stocks` renders **only** `FnoStockScanners`. Documented as actually wired.

MOUNT POINTS: `OptionsFlowCard.tsx` and `OptionsCopilotCard.tsx` both mount inside `FnoTab` (overview tab only, lines 155 and 177). `OptionsCopilotCard` is *also* imported by `components/fno/FoStrategiesWorkspace.tsx` (lab tab). Neither appears on analysis / stocks / oi / payoff.

SHARED CHROME (identical on all six tabs, rendered by page.tsx before TabsContent):
- AppShell 3-zone: fixed left sidebar 240px (`lg:ml-60`) / 68px collapsed (`lg:ml-[68px]`), fixed 72px right icon rail (`lg:mr-[72px]`), content `mx-auto w-full max-w-[1440px] px-4 md:px-6`. Mobile Topbar + MobileDrawer below `lg`. ⌘K CommandPalette. ConnectBrokerBanner above content, ComplianceFooter below.
- PageHeader: `flex flex-col gap-4 border-b border-line px-4 py-5 md:flex-row md:items-end md:justify-between md:px-6`. Eyebrow `font-mono text-[11px] uppercase tracking-[0.1em] text-d-text-muted` = "AI Derivatives Desk · F&O". H1 `text-display-sm font-normal truncate` = "F&O Desk" + Badge tone="warning" "Elite" (`bg-warning/10 text-warning border-warning/20`, rounded-full, px-2.5 py-0.5, text-xs/500). Description `mt-1 text-sm text-d-text-secondary` = "Agents read the derivatives tape live: index snapshots · per-stock OI scanners · option-chain heatmap · payoff explorer." Action = Button variant="ai" size md (`glass-control` #1E1E21 fill + 1px #29292D + ink #8FB0FF, h-8 px-3.5 text-[13px] rounded-sm 8px) with Sparkles `mr-1 h-4 w-4` + "Ask AI" → dispatchCopilotOpen('Given the current VIX and FII/DII flows, what option strategy fits a neutral-to-bearish Nifty view?').
- Body wrapper `space-y-6 px-4 py-5 md:px-6 xl:px-8` — DOUBLE horizontal gutter (AppShell already applies px-4 md:px-6), so real inset is 32px @md / 48px @xl.
- Tab rail: `<Reveal delay={0.08}>` (framer-motion opacity 0→1 + y 10→0, 450ms, cubic-bezier(.16,1,.3,1)) wrapping `<Card className="p-4">` = 12px radius, #151517 fill, 1px #29292D hairline, `shadow-none` (elevation flush), 16px padding. Inside: Radix TabsList `inline-flex items-center gap-1 rounded-full border border-line bg-main p-1` (pill rail on #0D0D0E). TabsTrigger `px-3.5 py-1.5 rounded-full text-sm font-medium` — inactive `text-d-text-muted`, active `bg-wrap text-d-text-primary shadow-sm`. TabsContent `mt-4`.
- Tab order + icons (all `mr-1 h-4 w-4`): Overview/Sparkles · Analysis/Sigma · Stock Scanners/ScanLine · OI Tracker/Layers3 · Payoff Calc/Sigma (Sigma reused) · Strategy Lab/Activity.
- Footer note `border-t border-line pt-4 text-[10px] text-d-text-muted`: "F&O data via admin Kite (option chain, indices) + NSE live FII/DII API. When provider feeds are down, panels show the source/error tag rather than synthetic numbers. Lot sizes: NSE Jan-2026 revision." Then `<DisclaimerFooter />` — **default `standard` variant**, i.e. "For educational purposes. Not investment advice. Markets carry risk." even though DisclaimerFooter's own docblock says the `derivatives` variant is for /fno. As-built drift.

ROUTING: `<Suspense fallback={null}>` wraps the body (useSearchParams requirement). `?tab=` is read ONCE into `useState` initial value — clicking a tab does NOT rewrite the URL, and changing `?tab=` without a remount does NOT switch tabs. Valid keys: overview | analysis | stocks | oi | payoff | lab; anything else falls back to overview. `/fo-strategies` 301s to `/fno?tab=lab`.

NESTING VIOLATION (as-built): the tab container is a Card at `bg-wrap` #151517, and nearly every inner panel is also `bg-wrap` / `bg-wrap/60` #151517 with a 1px #29292D hairline — card-in-card at the SAME surface tone. The documented rule is `.panel-inset`/`.tile-tint` (L2 #1E1E21, 12px radius, no border, no shadow). Only the `stocks`-tab empty state (`tile-tint`) and DerivativesAnalysis' `bg-surface-2` inputs follow it.

RADIUS DRIFT: `rounded-lg` (16px) is used for SnapshotCards, StrategyRows, the FII/DII strip and the bucket cards — 16px is the modal/sheet step, not the card step. `rounded-md` (12px) is used by OiHeatmap/PayoffCalculator/OptionsFlowCard. `rounded-xs` (6px) is used for chips, Stat tiles and the OiHeatmap toolbar. `.glass-card` (PayoffDiagram's shell) sets background/border/box-shadow but **no border-radius** in globals.css line 884-890, and PayoffDiagram adds no `rounded-*` utility — so the payoff panel renders with SQUARE 0px corners.

LEGACY COLOUR: PayoffDiagram's spot ReferenceLine uses `rgba(79,236,205,0.4)` stroke / `rgba(79,236,205,0.6)` label — a teal from a retired palette, not in the token set. Its tooltip background `rgba(13,15,14,0.95)` and grid `rgba(255,255,255,0.04)` are hardcoded dark values, so the chart does not adapt to light theme (tokens `--chart-tooltip-bg` #1E1E21/#FFFFFF and `--chart-grid` exist but are unused here).

TOKENS CONFIRMED IN globals.css: dark #0D0D0E/#151517/#1E1E21/#29292D/#3B3B40/#26262A/#0A0A0B · light #EDF1F4/#FFFFFF/#F4F7F9/#DDE5ED/#C8D4DE/#E7ECF1 · ink #F7F7F8/#D3D3D7/#96969E and #1D1D1D/#4D585F/#5F6B75 · primary FILL #406AE4, hover #3055C2, INK #8FB0FF dark / #3459C9 light · up #10B981/#0A6B50 · down #F5808C/#B81C22 · warning #F0A94F/#9A4D00 · --chart-primary #8FB0FF dark / #406AE4 light · radii 6/8/12/16/9999 · durations 90/160/240/380 · ease-out cubic-bezier(.22,1,.36,1).

FORMATTERS: `num()` = en-IN grouping (24,836.3 / 1,23,750). `numMax(v,n)` = en-IN, max n decimals, trailing zeros dropped. `qtyCompact()` = ≥1e7 "1.2 Cr", ≥1e5 "3.4 L", ≥1e3 "45.6 K" (used by DerivativesAnalysis). OiHeatmap and OptionsFlowCard use their OWN local compactors instead — OiHeatmap hardcodes `(n/1000).toFixed(0)+'k'` (so 12,40,000 renders "1240k", never lakh), OptionsFlowCard's `lots()` does ≥1e5→"1.2L", ≥1e3→"45k". Three different OI compaction schemes across one hub.

THEME: every screen is fully theme-aware through tokens EXCEPT the Recharts payoff canvas (hardcoded rgba dark values) and the `border-white/[0.08]` breakeven chip.

---

## `/fno?tab=overview` — F&O Desk — Overview (index snapshots + flow + copilot + strategy hints)

**File** `/Users/rishi/QuantX/frontend/app/(platform)/fno/page.tsx → /Users/rishi/QuantX/frontend/components/scanner/FnoTab.tsx (+ /Users/rishi/QuantX/frontend/components/fno/OptionsFlowCard.tsx, /Users/rishi/QuantX/frontend/components/fno/OptionsCopilotCard.tsx)` · **787 LOC** · **Access** Authed platform route. All four SWR calls are `auth: false` public endpoints (admin-Kite backed), so the tab renders without a connected broker — no BrokerLock on this tab.

**Shell** — AppShell 3-zone (240/68px sidebar · 72px right rail · max-w-[1440px] content) → PageHeader → Reveal → Card p-4 → Radix Tabs → TabsContent mt-4

**Purpose.** Default landing tab. One-screen read of the four NSE index option chains — spot, DTE, PCR + tag, max pain + distance, IV ATM/HV(20), IV rank/percentile, top OI strikes — plus a consolidated options-flow card, a deterministic plain-English "what this means" teacher, a user-triggered AI copilot, and rule-based strategy suggestions for the selected index.

### Anatomy (render order)

| # | Region | What renders |
|---|---|---|
| 1 | **Index selector bar** | `flex flex-wrap items-center gap-2 rounded-full border border-line bg-wrap/60 px-4 py-2` (9999px pill, #151517@60% on the #151517 card → reads as hairline-only). Left micro label "Index" `text-[10px] font-semibold uppercase tracking-wider text-d-text-muted`. Four buttons NIFTY · BANKNIFTY · FINNIFTY · MIDCPNIFTY, `rounded-full px-3 py-1 text-[11px] font-medium`; active = `.glass-control-accent` (#406AE4 fill, 1px #406AE4, white ink, elev-1), inactive = `.glass-control` (#1E1E21 fill, 1px #29292D, ink #D3D3D7 → #F7F7F8 on hover). A null snapshot disables the button, appends `(off)` in `ml-1 text-[10px]` and applies `opacity-40 cursor-not-allowed`. |
| 2 | **Selector bar — right cluster** | `ml-auto flex items-center gap-2 text-[10px] text-d-text-muted`: Badge tone="muted" "VIX · 12.84" (2dp), then a regime pill `rounded-full border px-2.5 py-0.5 text-[10px] capitalize` mapped by REGIME_TONE — complacent `bg-warning/10 text-warning border-warning/40`, normal `bg-primary/10 text-primary border-primary/40`, elevated `bg-up/10 text-up border-up/40`, stressed `bg-down/10 text-down border-down/40`, unknown `bg-d-text-muted/10 text-d-text-muted border-line`. Then a ghost Button size=sm (h-7 px-3) with RefreshCw `h-3.5 w-3.5`, `animate-spin` while either SWR key is loading, aria-label "Refresh"; onClick refetches BOTH keys. |
| 3 | **4-up SnapshotCard grid** | `grid grid-cols-1 gap-3 md:grid-cols-2 xl:grid-cols-4`. Each card is a `<button type="button">` — `rounded-lg border bg-wrap p-4 text-left transition-colors`; selected `border-primary bg-primary/5`, else `border-line hover:border-wrap-line` (#3B3B40). 16px radius, 16px padding. |
| 4 | **SnapshotCard interior** | Row 1 `flex items-baseline justify-between`: symbol `text-xs font-semibold uppercase tracking-wider text-d-text-secondary`; right `font-mono text-[10px] text-d-text-muted` "3 DTE" (— when null). Row 2 spot `mt-1 font-mono text-lg font-medium tabular-nums text-d-text-primary` "₹24,836.3" (numMax 2). Then `mt-2 grid grid-cols-2 gap-2 text-[11px]` holding FIVE cells — so the 2-col grid is 3 rows with an empty bottom-right slot. |
| 5 | **SnapshotCard cell — PCR** | Label `text-[9px] uppercase tracking-wider text-d-text-muted` "PCR"; value `font-mono tabular-nums` 2dp "0.92"; tag pill `mt-0.5 inline-block rounded-full border px-2 py-0.5 text-[9px] capitalize` with the underscore replaced by a space ("extreme bullish"). PCR_TAG_TONE: extreme_bullish `border-up bg-up/10 text-up` · bullish `border-up/60 bg-up/5 text-up` · normal `border-line bg-main text-d-text-secondary` · bearish `border-down/60 bg-down/5 text-down` · extreme_bearish `border-down bg-down/10 text-down`. |
| 6 | **SnapshotCard cell — Max Pain** | "MAX PAIN" micro label; value numMax(v,0) "24,800" or "—"; distance line `font-mono text-[10px]` — when max_pain_distance_pct > 0 renders `▼ 0.2%` in text-down, when ≤ 0 renders `▲ 0.2%` in text-up (abs, 1dp). Note the arrow/colour is inverted relative to price direction: a POSITIVE distance shows the red down-arrow. |
| 7 | **SnapshotCard cell — IV ATM / IV Rank / Top Strikes** | IV ATM: `(iv_atm*100).toFixed(1)+'%'` → "11.4%"; sub-line `font-mono text-[9px] text-d-text-muted` "HV(20) 9.8%". IV Rank: `iv_rank.toFixed(0)` "34" plus inline `ml-1 text-[9px] text-d-text-muted` "· 41%ile". Top Strikes: two `font-mono text-[10px]` lines — "S: 24800, 24700" in text-up (top 2 put-OI strikes, 0dp, comma-joined) and "R: 25000, 25100" in text-down; "—" when the array is empty. |
| 8 | **SnapshotCard — pull-to-max-pain flag** | When `pull_to_max_pain_signal`: `mt-2 rounded-full border border-primary/40 bg-primary/5 px-2.5 py-1 text-[10px] text-primary` with literal emoji copy "🎯 Pull-to-MaxPain signal active". |
| 9 | **SnapshotCard — loading / null variants** | Loading: `rounded-lg border border-line bg-wrap p-4` containing `<Skeleton w="100%" h="120px" />` (bg-line/80, animate-pulse, rounded-xs). Null snapshot: same shell at `opacity-60` with symbol `text-xs font-medium text-d-text-primary` and `mt-1 text-[10px] text-d-text-muted` "Snapshot unavailable." |
| 10 | **OptionsFlowCard** | `rounded-md border border-line bg-wrap p-4`. Header row: Layers `h-3.5 w-3.5 text-primary` + h3 `text-[12px] font-semibold text-d-text-primary` "Options Flow"; right caption `text-[10px] text-d-text-muted` "Consolidated OI positioning — descriptive, not a recommendation." Own independent index toggle (`mt-3`, 4 buttons `rounded-xs px-3 py-1 text-[11px] font-medium`, 6px radius, active = glass-control-accent) — NOT synced to the FnoTab selector, so this card can show BANKNIFTY while the page shows NIFTY. Refresh at `ml-auto`: `.glass-control rounded-xs p-1.5` with RefreshCw h-3.5. |
| 11 | **OptionsFlowCard — lean banner** | `flex items-center justify-between rounded-xs border px-3 py-2` tinted by LEAN_TONE: bullish `border-up bg-up/10 text-up`, bearish `border-down bg-down/10 text-down`, neutral `border-line bg-main text-d-text-secondary`. Left: TrendingUp/TrendingDown/Layers `h-4 w-4` + `text-[12px] font-semibold capitalize` "bullish lean". Right `text-[10px] opacity-80` "PCR bullish · writing bearish". |
| 12 | **OptionsFlowCard — 4 Stat tiles** | `grid grid-cols-2 gap-3 sm:grid-cols-4`. Each tile `rounded-xs border border-line bg-main px-3 py-2` (#0D0D0E fill inside the #151517 card — an inset well): label `text-[9px] uppercase tracking-wider text-d-text-muted`, value `mt-0.5 font-mono text-sm font-medium tabular-nums`, hint `mt-0.5 text-[9px] text-d-text-muted`. Exactly: "Put writing" / lots() / text-up / "fresh PE OI (floor)" · "Call writing" / text-down / "fresh CE OI (cap)" · "Net PCR" / 2dp / "PE OI / CE OI" · "Max pain" / numMax 0 / "▲ 0.4% from spot" or "pull to expiry". lots(): ≥100000 → "1.2L", ≥1000 → "45k", else raw. |
| 13 | **OptionsFlowCard — biggest OI move** | `rounded-xs border border-line bg-main px-3 py-2 text-[11px] text-d-text-secondary`; micro label "Biggest OI move" `text-[9px] uppercase tracking-wider`; body `mt-0.5 font-mono` = side (PE → text-up, CE → text-down) + strike numMax 0 + " · " + direction ("writing"/"unwinding") + signed oi_change (positive text-up, negative text-down) → "PE 24800 · writing +1.2L". |
| 14 | **Options Teacher block** | Renders only when `detail.teach.length`. `rounded-lg border border-line bg-wrap p-4`; header Sparkles `h-3.5 w-3.5 text-primary` + h3 `text-[12px] font-semibold` "What this means · NIFTY"; `ul.space-y-1.5` with li `flex gap-2 text-[12px] leading-relaxed text-d-text-secondary` and a `mt-0.5 text-primary` "•" bullet. Server-generated deterministic strings, 0 LLM tokens. |
| 15 | **OptionsCopilotCard** | `rounded-md border border-line bg-wrap p-4`. Header Sparkles + "AI Options Copilot" `text-[12px] font-semibold`; right "Descriptive — not a trade recommendation." Symbol toggle is only THREE indices — NIFTY · BANKNIFTY · FINNIFTY (no MIDCPNIFTY), `rounded-xs px-3 py-1 text-[11px]`. CTA at `ml-auto`: `flex items-center gap-1.5 rounded-full glass-control-accent px-3 py-1 text-[11px] font-medium active:scale-[0.98] disabled:opacity-60`, icon Sparkles or spinning Loader2, label "Get today's best trade" in the idle state and "Refresh" in every other state. Never fires on mount. |
| 16 | **OptionsCopilotCard — result** | Fact chips `flex flex-wrap items-center gap-2 text-[10px] text-d-text-muted`, each `rounded-xs border border-line bg-main px-2 py-0.5`: "VIX · 12.84", regime capitalized, "PCR · 0.92", "Max Pain · 24,800", "3 DTE". Optional LLM narrative `text-[12.5px] leading-relaxed text-d-text-secondary`. Then `ul.space-y-2` of CopilotStrategyRow: `rounded-xs border p-3`, head row = `border-primary/60 bg-primary/5` with a "BEST" chip `rounded border border-primary/60 bg-primary/10 px-1.5 py-0.5 text-[9px] font-semibold uppercase tracking-wider text-primary`; the rest `border-line bg-wrap`. Empty state copy: "No clear best trade right now — the option chain is unavailable or no rule fired for this regime." |
| 17 | **Strategy suggestions section** | `section.space-y-2`; header `flex items-center justify-between` with h3 `text-sm font-medium text-d-text-primary` "Strategy suggestions · NIFTY" and right caption `text-[10px] text-d-text-muted` "Descriptive — not trade recommendations. AutoPilot routing is opt-in (Phase 3)." |
| 18 | **StrategyRow** | `li.rounded-lg border border-line bg-wrap p-4` (16px radius). Head `flex flex-wrap items-baseline justify-between gap-2`: bias icon (bullish TrendingUp text-up · bearish TrendingDown text-down · else Layers text-d-text-muted, all h-3.5) + name `text-sm font-medium` + confidence pill `rounded-full border px-2 py-0.5 text-[10px] capitalize` (high `border-up bg-up/10 text-up` · medium `border-primary/60 bg-primary/5 text-primary` · low `border-line bg-main text-d-text-muted`); right `font-mono text-[11px] text-d-text-secondary` "~₹85k margin" (margin_estimate_inr ÷ 1000, 0dp). Rationale `mt-1 text-xs text-d-text-secondary`. Legs `ul.mt-2 space-y-0.5 text-[11px]` each `font-mono` "• SELL 25000 CE @ 118.50". Risk notes `ul.mt-2 space-y-0.5 border-t border-line/60 pt-2 text-[10px] text-d-text-muted` prefixed "⚠ ". Source label `mt-1 text-[9px] text-d-text-muted opacity-70`. |
| 19 | **Strategy suggestions — empty / loading / error** | Loading: 3 × `<Skeleton w="100%" h="80px" />` in `space-y-2`. Empty: `.tile-tint px-6 py-8` (#1E1E21, 12px radius, no border) containing `next/image` `/v4/illus/screener.png` 220×220 `mx-auto mb-2 w-full max-w-[220px] rounded-lg` above an EmptyState size="sm" (Sparkles icon in a 36px `h-9 w-9` ringed circle) titled "No strategy hits right now" with "VIX regime + PCR + IV configuration didn't trigger any rule." Detail error: EmptyState tone="error" size="sm" "Strategy suggestions failed" + handleApiError text. |
| 20 | **Whole-tab guard states** | allErr → EmptyState tone="error" (AlertTriangle h-6 w-6) "F&O snapshot unavailable" + Retry Button, replacing the entire tab. `allSnap && !allSnap.any_live` → EmptyState tone="warning" (Layers h-6 w-6) "Option chain provider offline" / "The admin Kite F&O feed is down. F&O snapshots return once it's restored." + Retry. EmptyState md shell: `flex flex-col items-center justify-center rounded-md bg-surface-2 gap-3 p-10`, 44px `h-11 w-11` icon circle `rounded-full border border-line bg-wrap`, title `text-[16px] leading-[22px] font-semibold`, description `mt-1.5 text-sm leading-[21px] text-d-text-secondary`, body capped `max-w-[42ch]`. |

**Components** — `app/(platform)/fno/page.tsx (FnoHubPage / FnoHubBody)` · `components/scanner/FnoTab.tsx` · `components/scanner/FnoTab.tsx → SnapshotCard (local)` · `components/scanner/FnoTab.tsx → StrategyRow (local)` · `components/fno/OptionsFlowCard.tsx` · `components/fno/OptionsFlowCard.tsx → Stat (local)` · `components/fno/OptionsCopilotCard.tsx` · `components/fno/OptionsCopilotCard.tsx → CopilotStrategyRow (local)` · `foundation/PageHeader` · `foundation/Card` · `foundation/Tabs + TabsList + TabsTrigger + TabsContent (Radix)` · `foundation/Badge` · `foundation/Button` · `foundation/EmptyState` · `foundation/Skeleton` · `foundation/Reveal (framer-motion)` · `foundation/DisclaimerFooter` · `next/image` · `lib/icons: Sparkles, Sigma, ScanLine, Layers3, Activity, Layers, RefreshCw, AlertTriangle, TrendingUp, TrendingDown, Loader2` · `lib/format: num, numMax` · `components/copilot/CopilotProvider → dispatchCopilotOpen`

**States & data.** THREE independent SWR subscriptions, all `revalidateOnFocus: false`. (1) key 'fno_snapshot_all' → GET /api/screener/fno/snapshot-all, dedupe 30 000ms, returns `{any_live, indices: Record<string, FnoIndexSnapshot|null>}`. (2) key ['fno_snapshot', selectedIndex] → GET /api/screener/fno/snapshot/{symbol}?include_strategies=true, dedupe 30 000ms, returns `{snapshot, teach?: string[], india_vix, regime, strategies?}`. (3) OptionsFlowCard key ['fno_options_flow', symbol] → GET /api/screener/fno/flow/{symbol}, dedupe 30 000ms. OptionsCopilotCard is NOT SWR — imperative `api.screener.fnoBestTrade(sym, true)` on button click with local state machine `'idle' | 'loading' | 'done' | 'empty'`. FnoIndexSnapshot fields consumed: symbol, spot, days_to_expiry, pcr_oi, pcr_tag (5-value union), max_pain, max_pain_distance_pct, pull_to_max_pain_signal, iv_atm, iv_rank, iv_percentile, hv.hv['20'], top_call_oi_strikes[], top_put_oi_strikes[]. Honest-empty contract: OptionsFlowCard returns `null` outright on error or empty; no synthetic numbers are ever rendered. Auto-refresh: none — no polling interval, refresh is manual via the RefreshCw buttons.

**Interactions.** Index chip click (selector bar) → setSelectedIndex, re-keys SWR (2), swaps the strategy list, Teacher block and the selected-card ring. Whole SnapshotCard is clickable and does the same. RefreshCw (selector bar) → `void refreshAll(); void refreshDetail()`, icon spins while either loads. OptionsFlowCard has its OWN index toggle + own refresh (independent state). OptionsCopilotCard index chips call selectSymbol → clears data and resets state to 'idle' (so the CTA label flips back to "Get today's best trade"); the CTA fires the request, disabled while loading, `active:scale-[0.98]` press. All chip/pill transitions are `transition-colors` at the default Tailwind duration; the accent controls use `--dur-instant` 90ms with ease-out cubic-bezier(.22,1,.36,1). Disabled offline index chips are non-interactive (`disabled` + cursor-not-allowed).

**Responsive.** Snapshot grid: 1 col base → `md:grid-cols-2` (768px) → `xl:grid-cols-4` (1280px). OptionsFlowCard stat tiles: `grid-cols-2` → `sm:grid-cols-4` (640px). Selector bar, copilot toolbar and every chip row use `flex-wrap` so pills reflow rather than scroll. Page gutter `px-4 py-5 md:px-6 xl:px-8` on top of AppShell's `px-4 md:px-6`. Sidebar/right-rail margins only apply at `lg` (1024px); below that the mobile Topbar + drawer take over and the content is full-bleed within the 16px gutter. Tab rail does NOT scroll horizontally — six triggers with icons will wrap or overflow the Card on narrow phones (`whitespace-nowrap` on each trigger, `inline-flex` list).

**Key copy.** "F&O Desk" · "Elite" · "AI Derivatives Desk · F&O" · "Agents read the derivatives tape live: index snapshots · per-stock OI scanners · option-chain heatmap · payoff explorer." · "Ask AI" · "🎯 Pull-to-MaxPain signal active" · "Snapshot unavailable." · "Options Flow" · "Consolidated OI positioning — descriptive, not a recommendation." · "fresh PE OI (floor)" / "fresh CE OI (cap)" · "PE OI / CE OI" · "pull to expiry" · "Biggest OI move" · "What this means · NIFTY" · "AI Options Copilot" · "Descriptive — not a trade recommendation." · "Get today's best trade" · "No clear best trade right now — the option chain is unavailable or no rule fired for this regime." · "BEST" · "Strategy suggestions · NIFTY" · "Descriptive — not trade recommendations. AutoPilot routing is opt-in (Phase 3)." · "No strategy hits right now" / "VIX regime + PCR + IV configuration didn't trigger any rule." · "F&O snapshot unavailable" · "Option chain provider offline" / "The admin Kite F&O feed is down. F&O snapshots return once it's restored." · "~₹85k margin"

<details><summary><b>Stitch prompt for this screen</b></summary>

```text
Dark fintech dashboard, page #0D0D0E, Geist sans + Geist Mono tabular numerics. Header band on #0D0D0E with 1px #29292D bottom hairline: mono 11px uppercase letterspaced 0.1em #96969E eyebrow "AI DERIVATIVES DESK · F&O"; 30px/600 #F7F7F8 title "F&O Desk" beside a 9999px amber pill "Elite" (#F0A94F ink on 10% tint); 14px #D3D3D7 subtitle; right an 8px-radius 32px-tall button, #1E1E21 fill, 1px #29292D, ink #8FB0FF, sparkle glyph + "Ask AI". Below, a 12px-radius #151517 card, 1px #29292D, 16px padding. Inside top-left a 9999px pill rail on #0D0D0E holding six 14px tabs; "Overview" active as a raised #151517 pill with soft shadow, the rest #96969E. Content 16px below. Row 1: a 9999px hairline bar labelled "INDEX" with four pills — NIFTY solid #406AE4/white, BANKNIFTY FINNIFTY MIDCPNIFTY on #1E1E21 — plus right chips "VIX · 12.84" and a blue "normal" pill and a refresh glyph. Row 2: four 16px-radius #151517 cards, 1px #29292D, 12px gutters. Card one, blue ring: "NIFTY" micro caps, "3 DTE" mono right, mono 18px "₹24,836.3", then a two-column 11px micro-label grid — PCR 0.92 with a green "bullish" pill, MAX PAIN 24,800 with red "▼ 0.2%", IV ATM 11.4% with "HV(20) 9.8%", IV RANK 34 · 41%ile, TOP STRIKES "S: 24800, 24700" green above "R: 25000, 25100" red. Below, an "Options Flow" card with a green "bullish lean" banner and four 6px-radius #0D0D0E stat wells: Put writing 4.2L green, Call writing 2.8L red, Net PCR 0.92, Max pain 24,800. Green #10B981, red #F5808C, blue #406AE4.
```
</details>

---

## `/fno?tab=analysis` — F&O Desk — EOD Derivatives Analysis (PCR gauge · max pain · OI walls · participants · ban list)

**File** `/Users/rishi/QuantX/frontend/components/fno/DerivativesAnalysis.tsx` · **597 LOC** · **Access** Public endpoints (auth:false). Broker state IS read via `useBrokerStatus()` — when the EOD symbol query errors/empties AND `!isConnected`, the symbol-driven cards are replaced by a frosted BrokerLock. Participant + ban cards render regardless of broker state.

**Shell** — Same AppShell + PageHeader + Card p-4 + Tabs rail; body is a `div.space-y-4` inside TabsContent mt-4

**Purpose.** Flagship end-of-day derivatives read for any NSE F&O symbol. Combines a one-line verdict, a PCR gauge, max-pain with a CE/PE OI split bar, a full by-strike OI-wall ladder with support/resistance detection, NSE participant positioning (FII highlighted) and today's F&O ban list — with a Beginner⟷Pro switch that hides every plain-language explainer in Pro mode.

### Anatomy (render order)

| # | Region | What renders |
|---|---|---|
| 1 | **Header card** | `.lg-surface rounded-md p-4 md:p-5` — #151517 fill, 1px #29292D, elev-1, 12px radius, 16px padding rising to 20px at md. Inner row `flex flex-wrap items-center justify-between gap-3`. |
| 2 | **Header — title block** | h2 `flex items-center gap-2 text-[18px] font-bold tracking-tight text-d-text-primary` with Sigma `h-4 w-4 text-primary` + "EOD F&O Analysis". Sub-line `mt-0.5 text-[11.5px] text-d-text-muted`: with data → symbol in `font-medium text-d-text-secondary` then " · 2026-08-01" (as_of) then " · 2026-08-07 expiry"; without data → "End-of-day options positioning, in plain English." |
| 3 | **Beginner ⟷ Pro segmented control** | `flex items-center rounded-sm border border-line bg-surface-2 p-0.5 text-[10.5px] font-medium` (8px radius, #1E1E21 track, 2px inner pad). Two buttons `rounded-xs px-2.5 py-1 transition-colors`; the active one gets `.glass-control-accent` (#406AE4 fill + white ink + elev-1), the inactive is `text-d-text-muted hover:text-d-text-primary`. Defaults to Beginner=true. |
| 4 | **Symbol search form** | `form.flex items-center gap-1.5`. Input `w-28 rounded-sm border border-line bg-surface-2 px-2.5 py-1.5 text-[12px] uppercase text-d-text-primary outline-none transition-colors placeholder:normal-case placeholder:text-d-text-muted focus:border-primary` — 112px wide, forces uppercase display, spellCheck off, placeholder "NIFTY". Submit `.glass-control-accent inline-flex items-center gap-1 rounded-full px-2.5 py-1.5 text-[12px] font-medium active:scale-[0.98] disabled:opacity-50` with a spinning Loader2 `h-3.5 w-3.5` while loading else Search `h-3.5 w-3.5`, label "Analyse". |
| 5 | **Verdict strip** | `mt-3 border-t border-d-border pt-3`. Single line `text-[13px] font-medium leading-snug`. Four exact strings: no data → "Search a symbol to read its option-market positioning." (text-d-text-secondary); pcr < 0.8 → "Options market leans bullish — more calls than puts, sellers expect the floor to hold." (text-up #10B981); pcr > 1.2 → "Options market leans bearish / hedged — puts outweigh calls into this expiry." (text-down #F5808C); else → "Options market is balanced — no decisive call/put tilt; price likely pins near max-pain." (text-d-text-primary). Loading swaps in `<Skeleton className="h-4 w-2/3 rounded bg-surface-2" />`. |
| 6 | **CardShell primitive (used ×5)** | `div.lg-surface rounded-md p-4`; head `mb-3 flex items-center justify-between gap-2`; h3 `flex items-center gap-1.5 text-[11px] uppercase tracking-wider text-d-text-muted` with a `h-3.5 w-3.5` lucide icon; optional right slot (InfoDot or an as_of timestamp `text-[10px] text-d-text-muted`). |
| 7 | **InfoDot popover** | Trigger `.glass-control inline-flex h-4 w-4 items-center justify-center rounded-full text-d-text-muted hover:text-d-text-primary` with HelpCircle `h-2.5 w-2.5`, aria-label "What does this mean?". Panel on click: `absolute right-0 top-5 z-20 w-56 rounded-sm border border-d-border bg-wrap p-2.5 text-[10.5px] leading-snug text-d-text-secondary shadow-lg` — 224px wide, 8px radius, z-index 20, dismissed on blur. |
| 8 | **Metrics bento** | `grid grid-cols-1 gap-4 lg:grid-cols-2` — PCR gauge left, Max Pain right at ≥1024px, stacked below. |
| 9 | **PCR gauge card** | Icon Activity, title "Put-Call Ratio (OI)". InfoDot copy: "PCR = open put OI ÷ call OI. Low (<0.8) = traders bought more calls (bullish or complacent). High (>1.2) = more puts (bearish or hedging)." Body: `flex items-end justify-between gap-3` with a `.numeric text-[30px] font-bold leading-none` value coloured by bucket (< 0.8 text-up, > 1.2 text-down, else text-d-text-primary) and a right block `text-right text-[10.5px] text-d-text-muted` reading "PCR-Vol 0.87" (value in `.numeric font-medium text-d-text-secondary`) then `mt-0.5` "scale 0 – 2 · center 1.0". |
| 10 | **PCR gauge bar** | `mt-3` → track `relative h-2.5 rounded-full bg-surface-2` (10px tall, #1E1E21). Centre tick `absolute left-1/2 top-1/2 h-3.5 w-px -translate-x-1/2 -translate-y-1/2 bg-d-text-muted/40` (14px × 1px). Knob `absolute top-1/2 h-4 w-4 -translate-x-1/2 -translate-y-1/2 rounded-full border-2 border-white shadow` (16px, 2px white ring), inline `left: clamp(3%, pcr/2*100%, 97%)`, `background` = var(--color-up) when < 0.8, var(--color-warning) when balanced, var(--color-down) when > 1.2, aria-label "PCR 0.92". Legend `mt-1.5 flex justify-between text-[9.5px] font-medium uppercase tracking-wider`: "Call-heavy" (text-up) · "Neutral" (text-d-text-muted) · "Put-heavy" (text-down). |
| 11 | **Max Pain card** | Icon Target, title "Max Pain". InfoDot copy: "Max pain is the strike where the largest number of option BUYERS lose money at expiry. Price often drifts toward it as expiry nears (the 'pinning' effect)." Value `.numeric text-[30px] font-bold leading-none text-d-text-primary` via numMax(v,2). Right block: "CE OI" + qtyCompact in `.numeric font-medium text-down`, then `mt-0.5` "PE OI" + qtyCompact in `.numeric font-medium text-up`. |
| 12 | **CE/PE split bar** | `mt-3` → `flex h-2 overflow-hidden rounded-full bg-surface-2` (8px tall). FIRST segment is `.bg-up` sized `total_pe_oi / (ce+pe) × 100%`, SECOND is `.bg-down` sized `total_ce_oi / (ce+pe) × 100%` — puts (green) grow from the left, calls (red) fill the right. Legend `mt-1.5 flex justify-between text-[9.5px] font-medium uppercase tracking-wider`: "Put OI (support)" text-up · "Call OI (resistance)" text-down. |
| 13 | **OI Walls ladder — header** | CardShell icon Layers3, title "OI Walls · support & resistance". InfoDot copy: "Each strike shows how much open interest sits there. Big PE walls (left, green) act as support — put writers defend that floor. Big CE walls (right, red) act as resistance — call writers cap the upside." Column head `mb-1 grid grid-cols-[1fr_auto_1fr] items-center gap-2 text-[9.5px] uppercase tracking-wider text-d-text-muted`: right-aligned "Put OI · support" in text-up · centred "Strike" · "Call OI · resistance" in text-down. |
| 14 | **OI Walls ladder — rows** | `div.space-y-[3px]` (3px row gap). Each row `grid grid-cols-[1fr_auto_1fr] items-center gap-2 text-[10.5px]`. Left cell `relative flex h-4 items-center justify-end` with bar `absolute right-0 h-3 rounded-l bg-up/45` (12px tall, 8px radius on the LEFT edge only, #10B981 @45%), width `pe_oi / maxOi × 100%` capped `maxWidth:100%`, label `relative z-10 mr-1 font-mono tabular-nums text-d-text-secondary` = qtyCompact (e.g. "3.4 L"). Centre cell `min-w-[3.5rem] text-center font-mono font-medium tabular-nums` (56px min) — text-up when it is the max-PE strike, text-down when it is the max-CE strike, else text-d-text-primary. Right cell mirrors with `absolute left-0 h-3 rounded-r bg-down/45`. maxOi = max of every ce_oi and pe_oi in the chain, floored at 1. |
| 15 | **OI Walls — support / resistance badges** | `mt-3 flex flex-wrap gap-2 border-t border-d-border pt-2.5 text-[10.5px]`: "Support 24,800" `inline-flex items-center gap-1 rounded-xs bg-up/10 px-2 py-0.5 font-medium text-up`; "Resistance 25,000" `bg-down/10 text-down`. Strikes via `num()` en-IN grouping. |
| 16 | **Participant Positioning card** | CardShell icon Users, title "Participant Positioning", right slot = `participants.as_of` in `text-[10px] text-d-text-muted`. Column head `grid grid-cols-[auto_1fr_auto] items-center gap-x-3 text-[9.5px] uppercase tracking-wider text-d-text-muted`: "Participant" · right-aligned "Options bull / bear" · right-aligned "Futures net". Rows `ul.mt-1.5 divide-y divide-d-border`, li same 3-col grid `py-1.5`; the FII row is highlighted `rounded-xs bg-primary/5 px-1.5` with its name in `text-[12px] font-semibold text-primary` (others `text-d-text-primary`). Middle cell `flex items-center justify-end gap-2 text-[10.5px] font-mono tabular-nums` = opt_bull text-up, "/" text-d-text-muted, opt_bear text-down. Right cell `text-right text-[12px] font-mono font-semibold tabular-nums` = "▲ 1.2 L" text-up when fut_net ≥ 0 else "▼ 84.3 K" text-down (abs qtyCompact). |
| 17 | **F&O Ban List card** | CardShell icon AlertOctagon, title "F&O Ban List", right slot ban.as_of. Chips `flex flex-wrap gap-1.5`, each `inline-flex items-center gap-1 rounded-xs border border-down/30 bg-down/10 px-2 py-1 text-[11px] font-medium text-down` with an AlertOctagon `h-3 w-3` and the symbol (e.g. IDEA, RBLBANK, GNFC). Empty: `py-2 text-[12px] text-d-text-secondary` "No stocks in F&O ban today." Loading: four `Skeleton h-6 w-16 rounded-xs bg-surface-2` chips. |
| 18 | **Plain-language lines (Beginner only)** | `<Plain>` renders `p.mt-2 flex gap-1.5 text-[11.5px] leading-snug text-d-text-secondary` with an Info glyph `mt-[2px] h-3 w-3 shrink-0 text-primary`. Every one vanishes when Pro is selected. Exact strings: PCR → "Put-Call Ratio 0.92: about 0.92 puts open for every call — balanced — no strong tilt." (bucket word is one of "call-heavy — bullish-leaning" / "balanced — no strong tilt" / "put-heavy — bearish / hedged"). Max pain → "Max pain 24,800: the strike where the most option buyers lose by expiry — price often drifts toward it." OI walls → "Biggest support 24,800 (put writers), biggest resistance 25,000 (call writers)." Participants → "FII are net long futures — the smart-money stance. Foreign desks are positioned for upside." (or short/downside), fallback "Net futures position per desk — ▲ long is bullish, ▼ short is bearish." Ban → "Banned = open interest maxed out; no new F&O positions allowed today, only square-off of existing ones." |
| 19 | **BrokerLock frosted state** | Fires when (error \|\| (!loading && !hasData)) && !isConnected. Outer `relative overflow-hidden rounded-md border border-line min-h-[300px]`. Behind: `<OptionChainPreview />` rendered TWICE inside `pointer-events-none absolute inset-0 select-none opacity-70 blur-[6px] saturate-[0.5]`. Overlay: `absolute inset-0 flex flex-col items-center justify-center bg-main/30 px-4 text-center backdrop-blur-[1.5px]` holding a 44px `h-11 w-11 rounded-full border border-line bg-wrap` circle with a Lock `h-5 w-5`, h3 `text-[15px] font-semibold` "Live derivatives data needs a connected broker", p `mt-1 max-w-sm text-[12.5px] leading-relaxed text-d-text-muted` "PCR, max pain, OI support/resistance and the NIFTY option chain stream from your own broker. Connect Zerodha, Upstox or Angel to unlock it.", and a Link `mt-4 inline-flex items-center gap-1.5 rounded-xs bg-primary px-3.5 py-1.5 text-[12.5px] font-medium text-primary-foreground hover:opacity-90` → /settings?tab=broker with Plug `h-3.5 w-3.5` + "Connect your broker". OptionChainPreview itself is a decorative 8-row `grid-cols-[1fr_auto_1fr]` of `h-2 rounded-full bg-up/40` and `bg-down/40` bars at widths [0.35,0.6,0.85,1,0.75,0.5,0.3,0.55] with "•••••" placeholder strikes — never real numbers. |
| 20 | **Non-lock error / empty** | Error with broker connected: `.lg-surface rounded-md p-6 text-center text-[12px] text-down` "Couldn't load F&O data for NIFTY — try again." No data: `.lg-surface rounded-md p-6 text-center text-[12px] text-d-text-muted` "No EOD F&O data yet for NIFTY. Try an index (NIFTY, BANKNIFTY) or an F&O stock." |
| 21 | **Card footer note** | `p.border-t border-line pt-3 text-[10px] text-d-text-muted`: "End-of-day derivatives data (nightly snapshot) · PCR, max-pain & OI walls from the option chain · participant OI from the NSE participant report. Honest-empty when a feed hasn't published. Not investment advice." |

**Components** — `components/fno/DerivativesAnalysis.tsx` · `DerivativesAnalysis → CardShell (local)` · `DerivativesAnalysis → InfoDot (local, click-toggle popover)` · `DerivativesAnalysis → Plain (local, Beginner-gated)` · `DerivativesAnalysis → pcrTone() (local tone bucketer)` · `components/broker/BrokerLock.tsx (default export)` · `components/broker/BrokerLock.tsx → OptionChainPreview (named export)` · `lib/hooks/useBrokerStatus` · `foundation/Skeleton` · `lib/format: num, numMax, qtyCompact` · `lib/icons: Activity, AlertOctagon, HelpCircle, Info, Layers3, Loader2, Search, Sigma, Target, Users, Lock, Plug` · `next/link`

**States & data.** NO SWR here — three imperative fetches. On mount a `useEffect` runs `loadEod('NIFTY')` plus `api.screener.fnoParticipants()` and `api.screener.fnoBan()` (both `.catch(() => setX(null))`). Local state: `input` (search text), `symbol` (last analysed, uppercased+trimmed), `eod`, `loading`, `error`, `beginner` (default true), `participants`, `ban`. GET /api/screener/fno/eod/{symbol} → `{success, symbol, expiry, as_of, metrics: {pcr_oi, pcr_volume, max_pain, total_ce_oi, total_pe_oi} | null, chain: Array<{strike, ce_oi, pe_oi}>}`. GET /api/screener/fno/participants → `{as_of, participants: Array<{participant, fut_net, opt_bull, opt_bear}>}`. GET /api/screener/fno/ban → `{as_of, symbols: string[]}`. Derived client-side: `pcrPct = clamp(3, 97, pcr/2*100)`; `maxOi = max(ce_oi, pe_oi) across chain, floor 1`; `topPe`/`topCe` = single-pass argmax over the chain (support/resistance strikes); `fii` = participants row whose `participant.toUpperCase() === 'FII'`. Loading skeletons: verdict `h-4 w-2/3`, both metric cards `h-20 rounded-sm bg-surface-2`, OI walls 8 × `h-4`, participants 4 × `h-6`, ban 4 × `h-6 w-16`. Empty strings: "No by-strike OI in the EOD chain." · "No participant OI data yet." · "No stocks in F&O ban today."

**Interactions.** Search form submit (Enter or the "Analyse" button) → `loadEod(input)`; the value is trimmed and uppercased before becoming `symbol`; blank input is a no-op; the button is `disabled` while loading and swaps Search for a spinning Loader2. Beginner/Pro toggle → instant show/hide of every `<Plain>` explainer, no refetch. InfoDot → click toggles the 224px popover, `onBlur` closes it (so a second click on another dot closes the first). Participant and ban cards never refetch after mount — there is no refresh control on this tab. The OI-wall ladder has no hover/tooltip affordance; bars are purely presentational. "Connect your broker" navigates to /settings?tab=broker.

**Responsive.** Header card row is `flex flex-wrap items-center justify-between gap-3`, so the toggle+search cluster drops under the title on narrow widths. Metrics bento and the participants/ban pair are both `grid grid-cols-1 gap-4 lg:grid-cols-2` — single column below 1024px. Header padding steps `p-4 md:p-5`. The OI-wall ladder keeps its `grid-cols-[1fr_auto_1fr]` at every width; the 56px min strike column plus 10.5px mono values is the narrowest it goes and it does NOT scroll horizontally. The whole chain renders unvirtualised with no max-height, so a 40-strike chain makes this the tallest tab.

**Key copy.** "EOD F&O Analysis" · "End-of-day options positioning, in plain English." · "Beginner" / "Pro" · "Analyse" · "Options market leans bullish — more calls than puts, sellers expect the floor to hold." · "Options market leans bearish / hedged — puts outweigh calls into this expiry." · "Options market is balanced — no decisive call/put tilt; price likely pins near max-pain." · "Search a symbol to read its option-market positioning." · "Put-Call Ratio (OI)" · "scale 0 – 2 · center 1.0" · "Call-heavy" / "Neutral" / "Put-heavy" · "Max Pain" · "Put OI (support)" / "Call OI (resistance)" · "OI Walls · support & resistance" · "Put OI · support" / "Strike" / "Call OI · resistance" · "Support 24,800" / "Resistance 25,000" · "No by-strike OI in the EOD chain." · "Participant Positioning" · "Options bull / bear" / "Futures net" · "F&O Ban List" · "No stocks in F&O ban today." · "Banned = open interest maxed out; no new F&O positions allowed today, only square-off of existing ones." · "Live derivatives data needs a connected broker" · "Connect your broker" · "Couldn't load F&O data for NIFTY — try again."

<details><summary><b>Stitch prompt for this screen</b></summary>

```text
Dark analytics screen, page #0D0D0E, cards #151517 with 1px #29292D borders and 12px radius, Geist sans plus Geist Mono tabular figures. Top card, 20px padding: 18px/700 #F7F7F8 heading "EOD F&O Analysis" with a blue #406AE4 sigma glyph, 11.5px #96969E sub-line "NIFTY · 2026-08-01 · 2026-08-07 expiry". Right side: an 8px-radius #1E1E21 two-button segment where "Beginner" is a solid #406AE4 white-ink chip and "Pro" is #96969E; beside it a 112px uppercase input on #1E1E21 reading NIFTY and a 9999px solid #406AE4 pill "Analyse" with a magnifier. Under a 1px #29292D rule, a 13px/500 #10B981 verdict: "Options market leans bullish — more calls than puts, sellers expect the floor to hold." Next a two-up grid, 16px gutter. Left card, 11px uppercase #96969E label "PUT-CALL RATIO (OI)": huge 30px/700 mono "0.78" in #10B981, right-aligned 10.5px "PCR-Vol 0.71" and "scale 0 – 2 · center 1.0"; below a 10px-tall #1E1E21 rounded track with a faint centre tick and a 16px green knob ringed 2px white at 39%, then micro caps "CALL-HEAVY" green, "NEUTRAL" grey, "PUT-HEAVY" #F5808C. Right card "MAX PAIN": mono 30px "24,800", right "CE OI 1.4 Cr" red over "PE OI 1.1 Cr" green, then an 8px split bar, green left, red right. Below, a full-width "OI WALLS · SUPPORT & RESISTANCE" ladder: rows of right-growing 12px green #10B981 45% bars labelled "3.4 L", centred mono strikes 24600 24700 24800 24900 25000, and left-growing red #F5808C 45% bars.
```
</details>

---

## `/fno?tab=stocks` — F&O Desk — Stock Scanners (FII/DII strip + 5 OI-buildup buckets)

**File** `/Users/rishi/QuantX/frontend/components/fno/FnoStockScanners.tsx` · **148 LOC** · **Access** Public endpoint (auth:false). No broker gate, no BrokerLock — buckets simply go empty when the NSE bhavcopy hasn't published, with an explicit warning banner instead of a lock.

**Shell** — Same AppShell + PageHeader + Card p-4 + Tabs rail; body is a `div.space-y-4` inside TabsContent mt-4

**Purpose.** Per-stock F&O signal board. Shows today's institutional FII/DII cash flow with its provenance, then five OI-buildup buckets — Long Buildup, Short Buildup, Short Covering, Long Unwinding, OI Spike — side by side, each capped to its top 12 symbols with price change and OI change. Explicitly honest about the OI feed being unavailable so an empty bucket is never mistaken for "no signal".

### Anatomy (render order)

| # | Region | What renders |
|---|---|---|
| 1 | **FII/DII institutional-flow strip** | `flex flex-wrap items-center gap-3 rounded-lg border border-line bg-wrap/60 px-3 py-2 text-xs` — 16px radius, 1px #29292D, 12px/8px padding. Leading micro label `text-[10px] font-semibold uppercase tracking-wider text-d-text-muted` "Institutional flow". |
| 2 | **Strip — flow figures** | Two inline spans: "FII" then value `font-mono tabular-nums` coloured text-up when fii_net ≥ 0 else text-down, rendered with an explicit "+" prefix when non-negative and the suffix " Cr" — e.g. "FII +1,234.5 Cr", "DII −456.7 Cr" (values via en-IN `num()`). |
| 3 | **Strip — provenance badges** | Badge tone="muted" carrying `fii_dii.date` or the literal fallback "unknown date"; then a Badge whose tone is "up" when `fii_dii.source === 'nse_live'` (green tint) else "muted", displaying the raw source string. Badges are `rounded-full border px-2.5 py-0.5 text-xs font-medium`. |
| 4 | **Strip — right cluster** | `ml-auto flex items-center gap-2 text-[10px] text-d-text-muted`: an OI-source Badge "OI · bhavcopy_2026-08-01" whose tone is "up" only when `oi_source.startsWith('bhavcopy')` else "muted"; then a ghost Button size="sm" with RefreshCw `h-3.5 w-3.5` (+`animate-spin` while loading), aria-label "Refresh". While data is undefined the whole strip body collapses to a single `<Skeleton w="60%" h="16px" />`. |
| 5 | **OI-feed warning banner** | Conditional — shown when data exists, `oi_source` does NOT start with 'bhavcopy', and `oi_last_error` is set. `rounded-md border border-warning/30 bg-warning/5 px-3 py-2 text-[11px] text-warning` (#F0A94F ink on a 5% tint with a 30% border). Copy: bold "NSE OI feed unavailable today:" then the raw error string, then " OI buildup buckets degrade to empty until the NSE bhavcopy publishes (usually post 5 PM IST). FII/DII flow still works via the live API." |
| 6 | **Bucket grid** | `grid grid-cols-1 gap-3 md:grid-cols-2 xl:grid-cols-3` holding FIVE cards — at xl that is 3 + 2, leaving a deliberate hole in the bottom-right cell. |
| 7 | **Bucket card shell** | `rounded-lg border border-line bg-wrap` (16px radius, no padding on the shell — each region pads itself). Header `flex items-center justify-between border-b border-line px-3 py-2`: icon `h-4 w-4` in the bucket tone + label `text-sm font-medium text-d-text-primary`; right `font-mono text-[10px] text-d-text-muted` "14 hits" (from `counts[key]`, 0 when absent). Help strip `border-b border-line/40 px-3 py-1 text-[10px] text-d-text-muted`. |
| 8 | **The five buckets (exact order, icon, tone, help copy)** | 1. "Long Buildup" — ArrowUpRight — text-up #10B981 — "Price ↑ + OI ↑ — fresh long positioning". 2. "Short Buildup" — ArrowDownRight — text-down #F5808C — "Price ↓ + OI ↑ — fresh short positioning". 3. "Short Covering" — ArrowUpLeft — text-up — "Price ↑ + OI ↓ — shorts squeeze-cover". 4. "Long Unwinding" — ArrowDownLeft — text-down — "Price ↓ + OI ↓ — longs covering (exhaustion warning)". 5. "OI Spike" — Zap — text-warning #F0A94F — "Single-symbol ΔOI ≥ 20% — institutional footprint". |
| 9 | **Bucket row list** | `ul.max-h-[260px] overflow-y-auto divide-y divide-line/40` — hard 260px scroll cap per bucket, rows sliced to the first 12. Each li `flex items-center justify-between gap-2 px-3 py-1.5 text-[11px]`: symbol `font-medium text-d-text-primary` (RELIANCE, TCS, HDFCBANK, INFY…); right group `flex items-center gap-2 font-mono tabular-nums text-d-text-secondary` with (a) price change `change_pct.toFixed(1)+'%'` prefixed "+" when ≥ 0, coloured text-up/text-down, and (b) "OI " + `oi_change_pct.toFixed(0)+'%'` prefixed "+" when ≥ 0, coloured text-warning when ≥ 0 else text-d-text-muted. |
| 10 | **Bucket empty / loading** | Empty: `px-3 py-6 text-center text-[11px] text-d-text-muted` "No matches right now." Loading: `space-y-1 p-3` with four `<Skeleton w="100%" h="20px" />` bars. |
| 11 | **Tab-level error** | Replaces the whole tab: EmptyState tone="error" with AlertTriangle `h-6 w-6`, title "F&O stock scanners failed", description = `handleApiError(error)`, action = Button "Retry" wired to `mutate()`. Rendered on the L2 recessed EmptyState shell (`rounded-md bg-surface-2 gap-3 p-10`, 44px icon circle). |

**Components** — `components/fno/FnoStockScanners.tsx` · `foundation/Badge` · `foundation/Button` · `foundation/EmptyState` · `foundation/Skeleton` · `lib/format: num` · `lib/icons: AlertTriangle, ArrowDownLeft, ArrowDownRight, ArrowUpLeft, ArrowUpRight, RefreshCw, Zap` · `swr (useSWR)` · `lib/api: api.screener.fnoStockScanners, handleApiError`

**States & data.** Single SWR subscription: key `'fno_stock_scanners'` → `api.screener.fnoStockScanners()` → GET /api/screener/fno/stock-scanners, options `{revalidateOnFocus: false, dedupingInterval: 60_000}` (60s dedupe, no polling). Response: `{fii_dii: {fii_net, dii_net, date, source, last_error}, oi_source: string|null, oi_last_error: string|null, buckets: {long_buildup, short_buildup, long_unwinding, short_covering, oi_spike}, counts: Record<string, number>}`. Each bucket row is `{symbol, change_pct, oi_change_pct, oi}` — note `oi` is fetched but NEVER rendered. Rendering guards: bucket rows default to `[]` and counts default to `0`, so a partial payload degrades per-card rather than throwing. Three distinct data-quality signals are surfaced: the FII/DII source badge (green only for 'nse_live'), the OI source badge (green only for a 'bhavcopy*' prefix), and the amber warning banner keyed on `oi_last_error`. There is no per-bucket error state — a failed request blanks the entire tab.

**Interactions.** RefreshCw ghost button in the strip → `mutate()` re-fetches the single key; the icon spins while `isLoading`. Retry button in the error EmptyState → the same `mutate()`. Each bucket list scrolls independently inside its 260px cap (native overflow-y). Rows are NOT clickable — no navigation to /stock/{symbol}, no hover state, no sort control, no bucket filter. Nothing else on this tab is interactive.

**Responsive.** Bucket grid 1 col base → `md:grid-cols-2` at 768px → `xl:grid-cols-3` at 1280px (5 cards, so the last row is short). The FII/DII strip is `flex-wrap`, so on narrow widths the FII/DII figures, date badge, source badge and the `ml-auto` refresh cluster stack into multiple lines and `ml-auto` stops pushing right once wrapped. Row content is `justify-between` at every width — symbol left, the two mono figures right — and the 11px mono type is the floor. No horizontal scroll anywhere.

**Key copy.** "Institutional flow" · "FII +1,234.5 Cr" / "DII −456.7 Cr" · "unknown date" · "OI · bhavcopy_2026-08-01" · "NSE OI feed unavailable today:" · "OI buildup buckets degrade to empty until the NSE bhavcopy publishes (usually post 5 PM IST). FII/DII flow still works via the live API." · "Long Buildup" / "Price ↑ + OI ↑ — fresh long positioning" · "Short Buildup" / "Price ↓ + OI ↑ — fresh short positioning" · "Short Covering" / "Price ↑ + OI ↓ — shorts squeeze-cover" · "Long Unwinding" / "Price ↓ + OI ↓ — longs covering (exhaustion warning)" · "OI Spike" / "Single-symbol ΔOI ≥ 20% — institutional footprint" · "14 hits" · "No matches right now." · "F&O stock scanners failed" · "Retry"

<details><summary><b>Stitch prompt for this screen</b></summary>

```text
Dark scanner board on #0D0D0E, Geist sans with Geist Mono tabular numbers. Top: a 16px-radius bar, #151517 fill, 1px #29292D, 12px by 8px padding, holding a 10px uppercase letterspaced #96969E label "INSTITUTIONAL FLOW", then 12px text "FII" beside mono "+1,234.5 Cr" in green #10B981 and "DII" beside mono "−456.7 Cr" in red #F5808C, then two 9999px pills — a grey "2026-08-01" on #1E1E21 and a green-tinted "nse_live" — and pushed right a green pill "OI · bhavcopy_2026-08-01" plus a 28px ghost refresh glyph. Below it an amber notice, 12px radius, #F0A94F ink on a 5 percent amber wash with a 30 percent amber border, 11px: "NSE OI feed unavailable today: bhavcopy 403. OI buildup buckets degrade to empty until the NSE bhavcopy publishes (usually post 5 PM IST)." Then a three-column grid of five 16px-radius #151517 cards, 12px gutters, each with a hairline header row: a 16px arrow glyph in the bucket colour, a 14px/500 #F7F7F8 title, and a mono 10px #96969E "14 hits" at right; under it a 10px #96969E help line on a fainter rule. Card one, green up-right arrow, "Long Buildup", help "Price ↑ + OI ↑ — fresh long positioning", rows divided by faint hairlines: RELIANCE with mono "+2.4%" green and "OI +31%" amber; TCS "+1.8%" / "OI +24%"; HDFCBANK "+1.1%" / "OI +19%"; INFY "+0.9%" / "OI +22%". Card two red down-right "Short Buildup". Card five amber lightning "OI Spike" showing "No matches right now." centred in 11px grey.
```
</details>

---

## `/fno?tab=oi` — F&O Desk — OI Tracker (strike-wise CE/PE open-interest ladder with ΔOI overlay)

**File** `/Users/rishi/QuantX/frontend/components/fno/OiHeatmap.tsx` · **157 LOC** · **Access** Public endpoint (auth:false) but broker-gated on failure: `useBrokerStatus()` — when the request errors or returns nothing AND `!isConnected`, the entire ladder is replaced by a frosted BrokerLock over a blurred decorative option-chain preview.

**Shell** — Same AppShell + PageHeader + Card p-4 + Tabs rail; body is a `div.space-y-4` inside TabsContent mt-4

**Purpose.** Strike-by-strike open-interest ladder for the four index chains. Renders mirrored horizontal bars — put OI growing right-to-left in green (support), call OI growing left-to-right in red (resistance) — with today's ΔOI printed inline beside each figure and the ATM strike row tinted blue. This is the tab where a trader reads where the writers are defending.

### Anatomy (render order)

| # | Region | What renders |
|---|---|---|
| 1 | **Toolbar** | `flex flex-wrap items-center gap-2 rounded-xs border border-line bg-wrap/60 px-3 py-2` — note `rounded-xs` = 6px here, unlike the overview tab's `rounded-full` selector bar. Micro label "Index" `text-[10px] font-semibold uppercase tracking-wider text-d-text-muted`, then four buttons NIFTY · BANKNIFTY · FINNIFTY · MIDCPNIFTY as `rounded-xs px-3 py-1 text-[11px] font-medium transition-colors`; active = `.glass-control-accent` (#406AE4 fill, white ink, elev-1), inactive = `.glass-control` (#1E1E21, 1px #29292D) with `text-d-text-secondary hover:text-d-text-primary`. Unlike FnoTab these chips are never disabled — there is no offline treatment. |
| 2 | **Toolbar — right cluster** | `ml-auto flex items-center gap-2 text-[11px] text-d-text-muted`: Badge tone="muted" "Spot ₹24,836.3" (numMax 2) and Badge tone="muted" "41 strikes" (`strike_count`), both only once data exists; then a ghost Button size="sm" with RefreshCw `h-3.5 w-3.5` (+ `animate-spin` while loading), aria-label "Refresh". |
| 3 | **Ladder card** | `rounded-md border border-line bg-wrap` — 12px radius, #151517 on the #151517 parent Card, separated only by the #29292D hairline. |
| 4 | **Ladder column header** | `flex items-center justify-between border-b border-line px-3 py-2 text-[10px] uppercase tracking-wider text-d-text-muted` with three spans: "Put OI · support" \| "Strike" \| "Call OI · resistance". IMPORTANT as-built detail: the header is a `flex justify-between` while the body rows are `grid grid-cols-[1fr_auto_1fr]`, so the three header labels do NOT align with the columns beneath them — the centre "Strike" label sits left of the actual strike column. |
| 5 | **Ladder scroll region** | `div.max-h-[600px] overflow-y-auto` — hard 600px cap, native scroll, unvirtualised, no sticky header (the header sits outside the scroller so it stays fixed). |
| 6 | **Strike row** | `grid grid-cols-[1fr_auto_1fr] items-center gap-2 border-b border-line/40 px-3 py-1 text-[11px]`; ATM rows additionally get `bg-primary/5` (#406AE4 at 5%). ATM test: `distance_pct != null && Math.abs(distance_pct) < 0.5` — i.e. any strike within ±0.5% of spot, so more than one row can highlight. |
| 7 | **Put OI cell (left)** | `relative flex h-5 items-center justify-end` (20px row cell). Bar `absolute right-0 h-3 rounded-l bg-up/40` — 12px tall, 8px radius on the LEFT edge only, #10B981 at 40% alpha (light theme #0A6B50 at 40%), inline `width: (put_oi / maxOi) * 100%` with `maxWidth: '100%'`. Label `relative z-10 mr-1 font-mono tabular-nums text-d-text-secondary` = `(put_oi/1000).toFixed(0)+'k'` → "1240k" for 12.4 lakh (a local compactor, NOT qtyCompact — it never rolls up to lakh/crore). |
| 8 | **Put ΔOI overlay** | Rendered inline after the OI figure only when `put_oi_change !== 0`: `<span className="ml-1 text-up\|text-down">` with a "+" prefix when positive, value `(put_oi_change/1000).toFixed(0)+'k'` → "+42k". Put mapping is INTUITIVE: positive ΔOI green (#10B981), negative red (#F5808C). |
| 9 | **Strike cell (centre)** | `text-center font-mono font-medium tabular-nums` using en-IN `num()` → "24,800". Auto-width (`auto` track), no min-width — narrower than the DerivativesAnalysis ladder's `min-w-[3.5rem]`. Inherits the row's `text-[11px]` and the default primary ink; unlike the analysis tab there is NO support/resistance colouring on the strike. |
| 10 | **Call OI cell (right)** | `relative flex h-5 items-center` (bar grows left→right). Bar `absolute left-0 h-3 rounded-r bg-down/40` — 12px tall, 8px radius on the RIGHT edge only, #F5808C at 40% (light #B81C22 at 40%), width `(call_oi / maxOi) * 100%` capped at 100%. Label `relative z-10 ml-1 font-mono tabular-nums text-d-text-secondary` = `(call_oi/1000).toFixed(0)+'k'`. |
| 11 | **Call ΔOI overlay — INVERTED colour** | Only when `call_oi_change !== 0`. `call_oi_change > 0` renders `text-down` (RED — fresh call writing = resistance building) and `< 0` renders `text-up` (GREEN — call unwinding = resistance lifting). This is the exact opposite mapping to the put side, which is deliberate semantic colouring (green always = bullish for price), not a bug — but it means a positive number can be red and a negative number green in the same row. |
| 12 | **Colour-mapping summary (record verbatim)** | There is NO heat ramp or per-cell gradient — this is a mirrored bar chart, not a true heatmap despite the filename. Exactly four fills are used: put bar `bg-up/40`, call bar `bg-down/40`, ATM row tint `bg-primary/5`, row divider `border-line/40`. Bar length is a linear share of `maxOi = max over all rows of max(call_oi, put_oi)`, floored at 1, memoised on `data`. Both bars are full-intensity at 40% alpha regardless of magnitude — magnitude is encoded only by length. |
| 13 | **Ladder footer** | `border-t border-line px-3 py-2 text-[10px] text-d-text-muted`: "Highlighted row = ATM. Put OI bars (green) flag support, Call OI bars (red) flag resistance. ΔOI overlay shows today's institutional flow per strike." |
| 14 | **Loading / error / lock states** | Loading (or no data yet, broker connected): a single `<Skeleton w="100%" h="400px" />` — a 400px pulsing block, no row skeletons. Error with broker connected: EmptyState tone="error" AlertTriangle `h-6 w-6`, title "OI heatmap unavailable", description `handleApiError(error)`, action Button "Retry" → `mutate()`. Error/empty with NO broker: BrokerLock feature="Live option chain", description "Strike-wise OI, PCR, max pain and support/resistance stream from your own broker. Connect Zerodha, Upstox or Angel to unlock the live chain.", `className="min-h-[320px]"`, wrapping `<OptionChainPreview />` — the preview is rendered twice inside `opacity-70 blur-[6px] saturate-[0.5]` with a `bg-main/30 backdrop-blur-[1.5px]` overlay carrying the 44px lock circle, the 15px/600 headline "Live option chain needs a connected broker", the 12.5px description and a solid #406AE4 6px-radius "Connect your broker" link to /settings?tab=broker. |

**Components** — `components/fno/OiHeatmap.tsx` · `components/broker/BrokerLock.tsx (default export)` · `components/broker/BrokerLock.tsx → OptionChainPreview` · `lib/hooks/useBrokerStatus` · `foundation/Badge` · `foundation/Button` · `foundation/EmptyState` · `foundation/Skeleton` · `lib/format: num, numMax` · `lib/icons: AlertTriangle, RefreshCw, Lock, Plug` · `swr (useSWR)` · `lib/api: api.screener.fnoOiHeatmap, handleApiError`

**States & data.** Local state: `symbol` (one of NIFTY | BANKNIFTY | FINNIFTY | MIDCPNIFTY, default NIFTY) plus `isConnected` from `useBrokerStatus()`. One SWR subscription keyed `['fno_oi_heatmap', symbol]` → `api.screener.fnoOiHeatmap(symbol)` → GET /api/screener/fno/oi-heatmap/{symbol}, `{revalidateOnFocus: false, dedupingInterval: 30_000}` — 30s dedupe, no polling. Response: `{symbol, spot, rows: Array<{strike, call_oi, put_oi, call_oi_change, put_oi_change, distance_pct}>, strike_count}`. Derived: `maxOi` via `useMemo` over `data.rows` reducing `max(call_oi, put_oi)` with a floor of 1 (so an all-zero chain doesn't divide by zero). Render branch order is: (error || (!isLoading && !data)) && !isConnected → BrokerLock; else error → EmptyState; else (isLoading || !data) → 400px Skeleton; else the ladder. All rows render — there is no strike-count cap, no virtualisation and no ATM-window filter, so the 600px scroller is the only bound.

**Interactions.** Index chip click → `setSymbol`, re-keys SWR, swaps the whole ladder (30s dedupe means a recently-viewed index returns instantly from cache). RefreshCw → `mutate()`, spins while loading. The 600px region scrolls natively; it does NOT auto-scroll to the ATM row on load, so on a 41-strike chain the highlighted ATM band is typically below the fold. Rows have no hover state, no tooltip, no click target and no sort. "Connect your broker" in the lock navigates to /settings?tab=broker.

**Responsive.** No responsive breakpoints inside this component at all — the toolbar is `flex-wrap` (chips reflow on narrow widths) and the ladder keeps `grid-cols-[1fr_auto_1fr]` with `px-3` at every viewport. The two 1fr bar columns simply compress, so on a phone the bars become very short while the mono labels stay 11px, and the header's `justify-between` labels drift even further out of alignment with the body columns. Inherits the page's `px-4 py-5 md:px-6 xl:px-8` gutter and the shell's `lg` sidebar/rail margins. Vertical bound is the fixed 600px scroller regardless of screen height.

**Key copy.** "Index" · "Spot ₹24,836.3" · "41 strikes" · "Put OI · support" / "Strike" / "Call OI · resistance" · "Highlighted row = ATM. Put OI bars (green) flag support, Call OI bars (red) flag resistance. ΔOI overlay shows today's institutional flow per strike." · "OI heatmap unavailable" · "Retry" · "Live option chain needs a connected broker" · "Strike-wise OI, PCR, max pain and support/resistance stream from your own broker. Connect Zerodha, Upstox or Angel to unlock the live chain." · "Connect your broker"

<details><summary><b>Stitch prompt for this screen</b></summary>

```text
Dark option-chain ladder on #0D0D0E, Geist Mono tabular figures throughout. Top toolbar: a 6px-radius bar, #151517 fill, 1px #29292D border, 12px by 8px padding, with a 10px uppercase letterspaced #96969E label "INDEX" then four 6px-radius chips — NIFTY solid #406AE4 with white ink, BANKNIFTY FINNIFTY MIDCPNIFTY on #1E1E21 with #29292D borders and #D3D3D7 ink — and pushed right two 9999px grey pills "Spot ₹24,836.3" and "41 strikes" plus a small refresh glyph. Below, a 12px-radius #151517 panel with a 1px #29292D border. Its header strip, separated by a hairline, holds three 10px uppercase #96969E labels: "PUT OI · SUPPORT" left, "STRIKE" centre-left, "CALL OI · RESISTANCE" right. Then about fourteen 20px-tall rows split by faint #29292D 40 percent hairlines, each a three-column layout. Left column: a 12px-tall green #10B981 bar at 40 percent opacity, rounded on its left end, anchored to the right edge and growing leftward, with an 11px mono #D3D3D7 label just inside reading "980k" followed by a green "+42k". Centre: an 11px mono #F7F7F8 strike — 24600, 24650, 24700, 24750, 24800, 24850, 24900, 24950, 25000, 25050. Right column mirrors: a 12px red #F5808C 40 percent bar rounded on its right end, growing rightward from the left edge, labelled "1240k" with a red "+88k". The 24850 row is tinted with a 5 percent #406AE4 wash to mark at-the-money. Footer strip in 10px #96969E: "Highlighted row = ATM. Put OI bars (green) flag support, Call OI bars (red) flag resistance."
```
</details>

---

## `/fno?tab=payoff` — F&O Desk — Payoff Calculator (leg builder + scenario sliders + expiry payoff chart)

**File** `/Users/rishi/QuantX/frontend/components/fno/PayoffCalculator.tsx + /Users/rishi/QuantX/frontend/components/strategy/PayoffDiagram.tsx` · **511 LOC** · **Access** Fully client-side maths — no auth, no broker, no market data required. The only network call is the public lot-size table; everything renders from seeded local state even offline (lot size falls back to 65).

**Shell** — Same AppShell + PageHeader + Card p-4 + Tabs rail; body is a two-pane `grid grid-cols-1 gap-4 lg:grid-cols-5` inside TabsContent mt-4

**Purpose.** Standalone multi-leg option payoff explorer. Pick an underlying, set spot, add up to 4 legs (direction, lots, strike, CE/PE, premium), then read max profit, max loss and breakevens off a live Recharts expiry-payoff curve. Scenario sliders shift spot ±10% and scale every leg premium 0.5×–2.0× as an IV proxy, redrawing the diagram in real time.

### Anatomy (render order)

| # | Region | What renders |
|---|---|---|
| 1 | **Two-pane layout** | `grid grid-cols-1 gap-4 lg:grid-cols-5`: `<aside className="lg:col-span-2 space-y-3">` (40% at ≥1024px) and `<section className="lg:col-span-3 rounded-md border border-line bg-wrap p-3">` (60%). Below lg the aside stacks above the chart. |
| 2 | **Underlying card** | `rounded-md border border-line bg-wrap p-3`. Micro label `mb-2 text-[10px] font-semibold uppercase tracking-wider text-d-text-muted` "Underlying". Four chips `flex flex-wrap gap-1.5`, each `rounded-xs px-2 py-1 text-[11px]` — active `.glass-control-accent`, inactive `.glass-control text-d-text-secondary hover:text-d-text-primary`. Keys come from SYMBOL_DEFAULTS in insertion order: NIFTY, BANKNIFTY, FINNIFTY, MIDCPNIFTY. |
| 3 | **Spot + lot-size inputs** | `mt-3 grid grid-cols-2 gap-2`. Left: label `block text-[10px] uppercase tracking-wider text-d-text-muted` "Spot ₹" over a `type="number"` input `mt-1 w-full rounded-xs border border-line bg-main px-2 py-1 text-sm font-mono tabular-nums text-d-text-primary outline-none focus:border-primary` (6px radius, #0D0D0E inset fill). Right: "Lot size" over a DISABLED input `bg-wrap-hover text-d-text-muted` (#1E1E21) — read-only, sourced from the API. |
| 4 | **SYMBOL_DEFAULTS (exact seeds)** | NIFTY {spot 24000, step 50} · BANKNIFTY {spot 51000, step 100} · FINNIFTY {spot 23000, step 50} · MIDCPNIFTY {spot 13000, step 25}. Switching symbol resets `spotPrice` to that seed AND re-rounds every existing leg strike to the new step (`Math.round(strike/step)*step`). |
| 5 | **Leg builder card** | `rounded-md border border-line bg-wrap`. Header `flex items-center justify-between border-b border-line px-3 py-2`: `text-[10px] font-semibold uppercase tracking-wider text-d-text-muted` "Legs (2/4)" (live count) and a ghost Button size="sm" with Plus `h-3.5 w-3.5`, `disabled` once `legs.length >= 4`. |
| 6 | **Leg row** | `li.space-y-2 p-3` inside `ul.divide-y divide-line/40`. Row 1 `flex flex-wrap items-center gap-1.5 text-[11px]`: a BUY/SELL `<select>` `rounded border border-line bg-main px-1.5 py-0.5`; a lots number input `w-12` (48px, min 1, clamped via `Math.max(1, …)`); a `text-[10px] text-d-text-muted` "×" separator; a strike number input `w-20` (80px) whose `step` is the symbol's strike step; a CE/PE `<select>`; a ghost Button with Trash2 `h-3 w-3`, aria-label "Remove leg". Row 2 `flex items-center gap-2 text-[11px]`: label "Premium ₹" `text-d-text-muted` + a `step="0.05"` number input `w-20`. All numeric inputs are `font-mono tabular-nums`. |
| 7 | **Default legs (seeded short straddle)** | Leg 1 — SELL 1 × 24000 CE @ 120. Leg 2 — SELL 1 × 24000 PE @ 130. New legs added by the + button seed as BUY 1 × {current spot} CE @ 50 with `id = String(Date.now())`. |
| 8 | **Net-premium footer** | `flex items-center justify-between border-t border-line px-3 py-2 text-[11px]`: "Net premium" `text-d-text-muted`; value `font-mono tabular-nums` text-up when ≥ 0 else text-down, formatted `{sign}₹{num(abs)}` → "+₹16,250". Computed as Σ over legs of `(direction === 'BUY' ? -1 : 1) × entry_price × lots × lotSize` — from the RAW legs, so it ignores the IV multiplier. |
| 9 | **Scenario analyzer card** | `rounded-md border border-line bg-wrap`; header `border-b border-line px-3 py-2 text-[10px] font-semibold uppercase tracking-wider text-d-text-muted` "Scenario analyzer"; body `space-y-3 p-3`. |
| 10 | **Scenario — spot shift slider** | Row `flex items-center justify-between text-[11px]`: label "Spot shift" `text-d-text-muted`; readout `font-mono tabular-nums text-d-text-primary` = signed 1dp percent + " → ₹" + adjusted spot 0dp, e.g. "+2.5% → ₹24,600". Input `type="range" min={-10} max={10} step={0.5}` with `mt-1 w-full accent-primary` (native track, #406AE4 thumb). |
| 11 | **Scenario — IV slider** | Label "IV shift (premium ×)"; readout `(ivMult*100).toFixed(0)+'%'` → "115%". Input `type="range" min={0.5} max={2.0} step={0.05}`, `accent-primary`. It multiplies every leg's `entry_price` before the diagram is computed (a premium proxy for an IV change) — it does NOT re-run any options model. |
| 12 | **Scenario — reset row** | `flex items-center justify-between text-[11px]`: a button `.glass-control rounded-xs px-2 py-1 text-[10px] text-d-text-secondary hover:text-d-text-primary` labelled "Reset scenario" (sets spotShiftPct 0, ivMult 1.0); right caption `text-[10px] text-d-text-muted` "Diagram updates live". |
| 13 | **Aside footer badge** | A standalone `<Badge tone="muted">` (rounded-full, `bg-wrap-hover text-d-text-muted border-line`, px-2.5 py-0.5, text-xs) reading "Educational tool — verify legs in your broker terminal before trading." — long copy inside a pill, so it wraps. |
| 14 | **PayoffDiagram shell** | `div.glass-card p-5` — the `.glass-card` rule in globals.css sets ONLY background #151517, 1px #29292D and elev-1; it declares NO border-radius and the component adds no `rounded-*` utility, so this panel renders with SQUARE 0px corners inside the 12px-radius section wrapper. 20px padding. |
| 15 | **PayoffDiagram title + summary badges** | h3 `text-sm font-semibold text-d-text-primary mb-1 flex items-center gap-2` with an inline 16px SVG trending-up-with-arrow (`w-4 h-4 text-primary`, strokeWidth 2, path `M3 13.5l6-6 4 4 8-8M21 3h-6m6 0v6`) then the label — "Custom strategy" from the calculator by default, or "Scenario: spot +2.5% · IV 115%" once either slider moves (the component's own fallback "Payoff at Expiry" never shows here). Badge row `flex flex-wrap gap-3 mb-4 mt-2`, each `text-[10px] px-2 py-0.5 rounded-full`: "Max Profit: ₹16,250" `bg-up/10 border border-up/20 text-up`; "Max Loss: ₹1,23,750" `bg-down/10 border border-down/20 text-down`; then one per breakeven "BE: 24,750" `bg-surface-2 border border-white/[0.08] text-d-text-secondary` (a hardcoded white-alpha border that does not adapt to light theme). |
| 16 | **Chart canvas + axes** | `div.h-[240px]` → Recharts `ResponsiveContainer width="100%" height="100%"` → `AreaChart`. X axis: `dataKey="price"`, ticks `fontSize 10, fill rgba(255,255,255,0.3)`, `tickFormatter = num(v)` → "24,800" en-IN. Y axis: same tick style, formatter `₹{sign}{abs≥1000 ? (abs/1000).toFixed(1)+'K' : abs}` → "₹16.3K", and negatives render as "₹-1.2L"-style "₹-123.8K" (the minus sits AFTER the rupee sign). CartesianGrid `strokeDasharray="3 3"` stroke `rgba(255,255,255,0.04)`. |
| 17 | **Chart annotations (record exactly)** | Three ReferenceLine families. (1) Zero P&L: `y={0}`, stroke `rgba(255,255,255,0.15)`, dash "4 4". (2) Spot marker: `x={spotPrice}` (the ADJUSTED spot), stroke `rgba(79,236,205,0.4)`, dash "4 4", label `{value:'Spot', position:'top', fill:'rgba(79,236,205,0.6)', fontSize:10}` — a legacy teal that exists in no token. (3) One per leg: `x={leg.strike}`, stroke `rgba(255,255,255,0.1)`, dash "2 2", unlabelled. Tooltip `contentStyle` background `rgba(13,15,14,0.95)`, border `1px solid rgba(255,255,255,0.08)`, borderRadius 8px, fontSize 12px; formatter → `['₹16,250','P&L']`; labelFormatter → "Spot: 24,800". |
| 18 | **Payoff series** | A single `<Area type="monotone" dataKey="pnl" stroke="var(--chart-primary)" fill="url(#payoffProfit)" strokeWidth={2} baseValue={0} isAnimationActive={false} />`. Stroke is #8FB0FF dark / #406AE4 light. Gradient `payoffProfit` = var(--chart-primary) 0.25 → 0 top-to-bottom. A second gradient `payoffLoss` (var(--color-down) 0.25 → 0, bottom-to-top) is DEFINED in `<defs>` but never referenced — so the loss region is filled with the same blue as the profit region, not red. Animation is off for instant slider response. |
| 19 | **Payoff maths** | Per-leg P&L = `(intrinsic − entry_price) × (BUY ? 1 : −1) × lots × lotSize`, intrinsic = `max(0, price − strike)` for CE and `max(0, strike − price)` for PE. Domain: min/max over strikes ∪ spot; `range = max−min \|\| spot*0.1`; `buffer = max(range*0.6, spot*0.08)`; `low = floor((min−buffer)/10)*10`; `high = ceil((max+buffer)/10)*10`; `step = max(1, round((high−low)/200))` → roughly 200 sample points. Max profit / max loss are the series extrema; breakevens come from sign-change detection between consecutive points with linear interpolation, rounded to the nearest integer, and can yield 0, 1 or 2 chips. |
| 20 | **Leg summary chips** | `mt-3 flex flex-wrap gap-2`; each `text-[10px] px-2 py-0.5 rounded-full border` — BUY `bg-up/5 border-up/15 text-up`, SELL `bg-down/5 border-down/15 text-down`. Copy: "SELL 24000 CE @ ₹120" plus " ×2" only when lots > 1. Because the calculator passes the IV-adjusted legs, these chips show the SCALED premium (e.g. "@ ₹138" at 115% IV), not the value typed into the leg builder. |
| 21 | **Empty states** | Calculator side, `legs.length === 0`: `flex h-64 items-center justify-center text-sm text-d-text-muted` "Add at least one leg to see payoff." (256px tall). PayoffDiagram's own guard (`!data.length`, e.g. spotPrice set to 0): `glass-card p-5 text-center` with `text-sm text-d-text-muted` "No legs configured for payoff diagram". |

**Components** — `components/fno/PayoffCalculator.tsx` · `components/strategy/PayoffDiagram.tsx` · `recharts: AreaChart, Area, XAxis, YAxis, Tooltip, ResponsiveContainer, CartesianGrid, ReferenceLine, linearGradient defs` · `foundation/Badge` · `foundation/Button` · `lib/format: num` · `lib/icons: Plus, Trash2` · `swr (useSWR)` · `lib/api: api.screener.fnoLotSizes`

**States & data.** Local state only, plus one cached lookup. `symbol` (key of SYMBOL_DEFAULTS, default NIFTY) · `spotPrice` (default 24000) · `legs: Leg[]` seeded with the two-leg short straddle · `spotShiftPct` (0) · `ivMult` (1.0). Derived via `useMemo`: `adjustedSpot = spotPrice * (1 + spotShiftPct/100)`; `adjustedLegs = legs.map(l => ({...l, entry_price: l.entry_price * ivMult}))`; `netPremium` from the raw legs. SWR key `'fno_lot_sizes'` → GET /api/screener/fno/lot-sizes, `{revalidateOnFocus: false, dedupingInterval: 86_400_000}` (24-hour dedupe — effectively a static table), response `{effective_from, lot_sizes: Record<string,number>, tick_sizes, note}`. `lotSize = lot_sizes[symbol] ?? 65` (the NIFTY Jan-2026 lot as the fallback). Leg cap is 4, matching the paper executor's broker leg limit. The `id` field is stripped before the legs reach PayoffDiagram. No validation beyond `lots >= 1` and `Number(x) || 0` coercion — a 0 or negative spot collapses the chart to PayoffDiagram's empty state.

**Interactions.** Underlying chip → resets spot to the seed AND re-rounds every leg strike to the new step (destructive, no confirm). Spot input → free numeric entry, recomputes the whole curve. + button → appends a BUY CE leg at the current spot @ ₹50, disabled at 4 legs. Trash2 → removes that leg; at 0 legs the right pane swaps to the 256px "Add at least one leg" message. Direction/type selects, lots, strike and premium inputs all update on change with no debounce — every keystroke recomputes ~200 payoff points and redraws. Both range sliders are live-dragging with `isAnimationActive={false}`, so the curve translates and rescales continuously; the diagram label switches to "Scenario: …" as soon as either leaves its default. "Reset scenario" returns both to 0% / 1.0×. Chart hover raises the Recharts tooltip ("Spot: 24,800" / "₹16,250 P&L"). The lot-size input is `disabled` and cannot be edited.

**Responsive.** Base: single column — the whole aside (underlying, leg builder, scenario, badge) stacks above the chart pane. At `lg` (1024px) it becomes a 5-column grid, aside 2/5 and chart 3/5. Inside the aside, the spot/lot pair is a fixed `grid-cols-2` at every width and the leg row is `flex-wrap`, so on a phone the direction select, lots, ×, strike, CE/PE and delete controls wrap onto two or three lines. The chart is a fixed `h-[240px]` at all widths with a fluid ResponsiveContainer — it never grows taller on desktop, so on a wide monitor the payoff curve is a short, wide band. Summary and leg chips are `flex-wrap`. No horizontal scroll.

**Key copy.** "Underlying" · "Spot ₹" / "Lot size" · "Legs (2/4)" · "Premium ₹" · "Net premium" · "Scenario analyzer" · "Spot shift" · "IV shift (premium ×)" · "Reset scenario" · "Diagram updates live" · "Educational tool — verify legs in your broker terminal before trading." · "Add at least one leg to see payoff." · "Custom strategy" · "Scenario: spot +2.5% · IV 115%" · "Max Profit: ₹16,250" / "Max Loss: ₹1,23,750" / "BE: 24,750" · "Spot" (chart annotation) · "P&L" / "Spot: 24,800" (tooltip) · "SELL 24000 CE @ ₹120" · "No legs configured for payoff diagram"

<details><summary><b>Stitch prompt for this screen</b></summary>

```text
Dark two-pane options calculator on #0D0D0E, Geist sans plus Geist Mono tabular numbers. Left column takes 40 percent and stacks three 12px-radius #151517 cards with 1px #29292D borders and 12px padding. Card one "UNDERLYING" in 10px uppercase #96969E, four 6px-radius chips — NIFTY solid #406AE4 white ink, BANKNIFTY FINNIFTY MIDCPNIFTY on #1E1E21 — then two fields side by side: "SPOT ₹" over a 6px-radius #0D0D0E input showing mono 24000, and a dimmed "LOT SIZE" input on #1E1E21 showing 65. Card two headed "LEGS (2/4)" with a small plus button, then two hairline-divided rows, each a wrapping line of tiny controls: a SELL dropdown, a 48px "1", a grey "×", an 80px "24000", a CE dropdown and a trash glyph; underneath "Premium ₹" with an 80px "120". Second row reads SELL 1 × 24000 PE @ 130. A footer strip: "Net premium" grey at left, mono "+₹16,250" in green #10B981 at right. Card three "SCENARIO ANALYZER" with two full-width native range sliders using #406AE4 accents — "Spot shift" reading "+2.5% → ₹24,600" and "IV shift (premium ×)" reading "115%" — plus a small "Reset scenario" chip and grey "Diagram updates live". Right pane, 60 percent: a #151517 panel with SQUARE corners, 20px padding. 14px/600 title with a blue trend arrow: "Scenario: spot +2.5% · IV 115%". Three 9999px 10px chips: green "Max Profit: ₹16,250", red "Max Loss: ₹1,23,750", grey "BE: 24,750". Then a 240px area chart: an inverted-V payoff curve stroked 2px in #8FB0FF over a fading blue wash, a dashed white zero line, a dashed teal vertical "Spot" marker, faint dashed strike lines, 10px grey axis ticks reading 23,200 · 24,000 · 24,800 · 25,600 across the bottom and ₹16.3K · ₹0 · ₹-123.8K up the side. Below, two 10px red pills: "SELL 24000 CE @ ₹138" and "SELL 24000 PE @ ₹150".
```
</details>

---

## `/fno?tab=lab` — F&O Desk — Strategy Lab (tab entry point for the folded F&O workspace)

**File** `/Users/rishi/QuantX/frontend/app/(platform)/fno/page.tsx (lines 126-158) → /Users/rishi/QuantX/frontend/components/fno/FoStrategiesWorkspace.tsx` · **2558 LOC** · **Access** Authed. Unlike every other tab this one is user-scoped: it reads and writes the signed-in user's paper multi-leg option positions (open, close, adjust) via the /api/fo-strategies/* endpoints, so it needs a session — the public screener endpoints the other five tabs use are not sufficient here.

**Shell** — Same AppShell + PageHeader + Card p-4 + Tabs rail; TabsContent mt-4 renders `<FoStrategiesWorkspace />` inline with no wrapper of its own

**Purpose.** Entry point for the full F&O workspace, re-homed inline after the /fo-strategies route was retired. This entry exists so users' open multi-leg paper option positions stay reachable and closeable at a stable deep link. Per the job split, only the tab entry is recorded here — the workspace body itself is documented by the separate fno-lab job.

### Anatomy (render order)

| # | Region | What renders |
|---|---|---|
| 1 | **Tab trigger** | Last of six TabsTrigger elements: `px-3.5 py-1.5 rounded-full text-sm font-medium whitespace-nowrap`, inactive `text-d-text-muted hover:text-d-text-primary`, active `data-[state=active]:bg-wrap data-[state=active]:text-d-text-primary data-[state=active]:shadow-sm`. Content = Activity icon `mr-1 h-4 w-4` + the label "Strategy Lab" (value key `lab`). |
| 2 | **Tab panel** | `<TabsContent value="lab" className="mt-4">` renders `<FoStrategiesWorkspace />` directly — no Card, no Panel, no padding wrapper. The workspace therefore paints edge-to-edge inside the 16px padding of the outer tab Card, and brings its own PageHeader/Tabs stack (it imports PageHeader, Tabs, TabsList, TabsTrigger, TabsContent, Dialog, StatCard, NumericInput and toast), producing a nested header-inside-a-header and a second tab rail 16px below the hub's tab rail. |
| 3 | **Deep link** | /fno?tab=lab is the 301 target of the retired /fo-strategies route. `?tab=` is read once into the `useState` initial value inside a `<Suspense fallback={null}>` boundary, so a cold navigation to /fno?tab=lab opens directly on this tab; switching tabs afterwards does NOT update the URL, and the browser back button will not return to a previous tab. |
| 4 | **Documented workspace contents (from the source docblock, lines 152-157 of page.tsx and the FoStrategiesWorkspace header)** | Recommendation grid of live rule-based proposals — one card per symbol/strategy with view, breakevens, max profit/loss and probability of profit, each carrying a "Deploy to paper" button; an open paper-positions panel with 30-second live mark-to-market P&L, per-leg breakdown, Close and Adjust; a recently-closed book (last 10) as an audit trail; an option chain + leg builder; AI-suggest; and a backtest surface. Charting is Recharts ComposedChart with Line, Scatter, ReferenceLine, Legend and CartesianGrid. |
| 5 | **Shared components it re-mounts** | `OptionsCopilotCard` (the same component that appears on the overview tab) is imported and rendered inside the workspace, so the AI Options Copilot exists in two places in this hub. It also uses the AI token set from lib/tokens and `inrSigned`/`num`/`numMax` from lib/format. |
| 6 | **Backing endpoints** | GET /api/fo-strategies/overview (recommendations) · POST /api/fo-strategies/paper/open (deploy to paper) · GET /api/fo-strategies/paper/positions (open + recent, with MTM) · POST /api/fo-strategies/paper/{id}/close. SWR_RECS config: `{revalidateOnFocus: false, refreshInterval: 300_000, dedupingInterval: 60_000}` — recommendations refresh every 5 minutes; the positions key polls faster for live MTM. |
| 7 | **Shared page chrome below the panel** | Identical to the other five tabs: the `border-t border-line pt-4 text-[10px] text-d-text-muted` provenance note ("F&O data via admin Kite … Lot sizes: NSE Jan-2026 revision.") and `<DisclaimerFooter />` in its default `standard` variant — "For educational purposes. Not investment advice. Markets carry risk." Note the derivatives-specific wording ("Derivatives carry a high risk of loss and are not suitable for every investor.") exists in DisclaimerFooter but is NOT selected on this route despite the component's docblock naming /fno and the F&O Lab as its intended callers. |

**Components** — `app/(platform)/fno/page.tsx (TabsTrigger value="lab" + TabsContent value="lab")` · `components/fno/FoStrategiesWorkspace.tsx (2558 LOC — documented separately)` · `components/fno/OptionsCopilotCard.tsx (re-mounted inside the workspace)` · `foundation/Tabs, TabsList, TabsTrigger, TabsContent` · `foundation/DisclaimerFooter` · `lib/icons: Activity`

**States & data.** At the hub level the only state is `tab === 'lab'` in `FnoHubBody`'s `useState`. The panel is NOT lazily loaded — `FoStrategiesWorkspace` is a static top-of-file import in page.tsx, so its 2 558 lines plus the full Recharts ComposedChart bundle are in the /fno client chunk for every visitor, including someone who only ever opens the Overview tab. Radix TabsContent unmounts inactive panels by default, so the workspace's SWR subscriptions and its 30-second MTM polling start on first activation of this tab and stop when the user switches away — meaning open-position P&L is only live while the tab is on screen.

**Interactions.** Clicking the "Strategy Lab" trigger sets `tab` to 'lab' and mounts the workspace; the URL is unchanged. Landing on /fno?tab=lab from the /fo-strategies redirect mounts it directly. All further interaction (deploy to paper, close a position, adjust legs, run a backtest, AI-suggest legs) belongs to the workspace and is covered by the separate fno-lab job.

**Responsive.** Inherits the hub's `space-y-6 px-4 py-5 md:px-6 xl:px-8` body gutter, the outer Card's 16px padding and the AppShell's `lg:ml-60 lg:mr-[72px]` rails with a `max-w-[1440px]` cap. Because the workspace renders its own PageHeader and its own TabsList inside that padded Card, the nested rails compete for horizontal room at narrow widths — the hub's six-trigger rail and the workspace's own rail both sit in the same column below 768px.

**Key copy.** "Strategy Lab" (tab label) · shared footer "F&O data via admin Kite (option chain, indices) + NSE live FII/DII API. When provider feeds are down, panels show the source/error tag rather than synthetic numbers. Lot sizes: NSE Jan-2026 revision." · "For educational purposes. Not investment advice. Markets carry risk."

<details><summary><b>Stitch prompt for this screen</b></summary>

```text
Dark trading hub on #0D0D0E showing only the tab-entry state, Geist sans. At the top a header band with a 1px #29292D bottom hairline: mono 11px uppercase letterspaced #96969E eyebrow "AI DERIVATIVES DESK · F&O", a 30px/600 #F7F7F8 title "F&O Desk" beside a 9999px amber pill "Elite" in #F0A94F on a 10 percent tint, a 14px #D3D3D7 description line, and at right an 8px-radius 32px button on #1E1E21 with a 1px #29292D border and #8FB0FF ink reading "Ask AI" with a sparkle. Below, a 12px-radius #151517 card with a 1px #29292D border and 16px padding. Along its top edge a 9999px pill rail on a #0D0D0E fill with 4px inner padding, holding six 14px/500 tabs each with a 16px leading glyph: Overview, Analysis, Stock Scanners, OI Tracker, Payoff Calc, and Strategy Lab. "Strategy Lab" is the ACTIVE one — a raised #151517 pill with #F7F7F8 ink and a soft drop shadow, carrying a pulse-activity glyph; the other five are flat #96969E with no fill. Sixteen pixels beneath the rail, the panel region is empty and awaiting the embedded workspace: show it as a large open area on the card fill with no placeholder illustration. At the bottom of the page, above a 1px #29292D rule, an 10px #96969E provenance line "F&O data via admin Kite (option chain, indices) + NSE live FII/DII API. Lot sizes: NSE Jan-2026 revision." and a centred 10px grey disclaimer "For educational purposes. Not investment advice. Markets carry risk." Blue accent #406AE4, green #10B981, red #F5808C reserved for P&L only.
```
</details>

---
